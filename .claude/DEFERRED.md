# Deferred work — the ledger of what is NOT done

**Why this file exists.** Twice in one session, work that was never done read as done. The P3
remediation lane died without pushing and the tracker went on listing it as in flight. An
`actions.md` entry said the "Re-run ATS analysis" button could not re-run months after it was fixed.
Both failures are the same shape: **a claim about state that nothing re-checks.**

Every row below is something a reader could reasonably assume is finished and is not. Each names
**what makes it look done** — because that is the thing that will mislead the next session — and the
**trigger** that should make someone pick it up.

Rules:
1. A row leaves this file only when the work is done AND verified, or when the owner decides it will
   never be done — in which case its status becomes `WONTDO` with the reason, never just deleted.
2. Adding a row is part of the commit that defers the work. Deferring silently is the failure.
3. `git branch -r` is the proof a lane exists. A tracker entry is not.

**This file is machine-checked by `api/test/deferredLedger.test.mjs`.** Prose said "CLOSED" here
while other rows said "DONE" and "FIXED", so nothing could tell open from closed, four IDs meant two
things each, and a bad merge left a section duplicated verbatim. A ledger that exists to catch stale
claims went stale three times. The format below is what makes it checkable — a malformed or stale row
now fails the suite.

**Status** is one of `OPEN`, `CLOSED`, `WONTDO`. It is a token, never inferred from prose. A row with
any part still outstanding is `OPEN`, however much of it is finished — "DONE, but not verified live"
is exactly the claim this file exists to catch, so it is OPEN.

**IDs.** `D1`-`D37` are frozen, the same way `H1`-`H44` are (see `H26`): they are cited from
`.claude/actions.md`, from commit messages and from each other. The counter is retired — **every new
row takes a slug**, `D:two-or-more-words` saying what it defers. Four numbers had already come to
mean two things each (`D20` `D21` `D22` `D26`); the later claimant of each was re-slugged, and a new
numeric id now fails the suite.

**Every `OPEN` row carries exactly one `check:` directive** in its last column, and the suite RUNS it:

| Directive | Row stays valid while | Fails when |
|---|---|---|
| `check: grep <path> <regex>` | the pattern is still PRESENT | it no longer matches — the defect is gone, so the row is stale |
| `check: absent <path> <regex>` | the pattern is still ABSENT | it now matches — the thing was built, so the row is stale |
| `check: manual <vehicle> — <reason>` | — | the vehicle is not one of `db-query.yml`, `api-test.yml`, `ui-verify.yml`, `owner`, `lane` |

There is no fourth kind: an owner decision is `check: manual owner — …`, so it is validated like any
other. Write a literal pipe in a pattern as `\|`; an unescaped one splits the markdown row.

**A pattern must be able to go false, or it is not a check.** Three of the first four written here
could not, and two were proven inert by reinstating the exact regression the row names while the
suite stayed green: `D2` grepped `generalize`, which survives the import, the type literal and a
comment after the last CALLER is gone (now `= generalize\(`); `D10` grepped `H26`, which survives in
comments after the case itself is deleted (now `test\('H26:` — `H26` strips comments before scanning
for exactly this reason, and this check had done the opposite); `D14`'s defect is what `covered_kw`
MEANS, which no source grep can settle, so it is `manual`. An empty pattern compiles to `/(?:)/`,
matches every file, and is rejected.

A `manual` row is not a passing row. It is a row nothing in this sandbox can settle, so it must name
the vehicle that WOULD settle it — absent evidence is `not_applicable`, never `pass`. The census
prints all three buckets on every run — machine-checked, manual, and **closed with no check at
all** — because the first version printed the first two and omitted the third, so 16 rows closed by
prose alone read as covered by a green run.

---

## P8.2 / R3 — figure echo

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D1 | CLOSED | ~~The rewrite half of R3 does not exist~~ **CLOSED.** `applyCorrectionPass` runs in `ensurePackage` before the `pkg_json` write; corrections are stored and revertible. Verified in production: table, six constraints and the routes are live. | — | — |
| D2 | CLOSED | ~~`generalize()` has zero production callers~~ **CLOSED.** Called from `planCorrections` (`correction.ts:71`), which the pass calls per field. | — | `check: grep api/src/functions/tests/correction.ts = generalize\(` |
| D3 | OPEN | **OPEN — the AC-10 profile-figure resolver is not built.** The substitute-figure half is settled as 'generalization only', which is already what ships. The gap is structural and stays that way deliberately: `scanEcho`'s profile index is keyed on the EXACT figure, so a hit is the KEEP branch (`shared_with_profile`) — the opposite of a substitute — and answering "what is the candidate's corresponding number?" needs a unit-keyed profile index that does not exist. `P8.1-ACCEPTANCE.md` §4 already recorded generalization-only as the accepted resolution, so the deliverable was not code but three guards that make the forbidden version fail a test: `H:no-figure-ranking` (Math.abs banned — 0 uses today; every `.sort()` in the figure path must order by document offset; names matched WITHOUT a trailing boundary), `H:generalize-closed-range` (the range is `N-figure`, `multiple`, or `null`, so a bare quantity can never be output), `H:profile-figure-provenance` (the AC-10 tripwire). **The first version of `H:no-figure-ranking` was INERT** — it banned `/\bnearest\b/`, which does not match `nearestProfileFigure` because the trailing `\b` fails on a camelCase capital, the same shape as the percent regex that never matched `40%`. Caught only by reinstating the defect. Rewritten to match the construct and re-proved against four resolver shapes; two of them passed the word-list version. | — | **Still open: the AC-10 resolver itself.** It needs `profile_source_key` / `profile_char_start` / `profile_char_end` on `correction`, i.e. `schema.ts` — another lane's file. Adding them to `ensureCorrectionTable` alone would recreate D21. `check: absent api/src/functions/tests/schema.ts profile_source_key` |
| D4 | CLOSED | ~~Non-numeric echoes are not detected~~ **CLOSED.** `scanWording` (figureEcho.ts) + the `posting_wording_kept` check. It only ever LISTS — nothing rewrites prose, and it is a SEPARATE check from `posting_figure_echo` because the remedy differs: a figure is auto-corrected by P8.1, a phrase never is, and folding them together would put prose into the auto-correct path. Cry-wolf was the whole design risk, so: exact contiguous tokens (no stemming — `stem` is deliberately unused on prose), an 8-token run, the run verified contiguous in the posting's own token stream rather than assembled from overlapping n-grams, wording the PROFILE also states dropped as the candidate's, a content-word floor so `and you will have to be able to` is not an accusation, and `not_applicable` for absent posting/profile. Run length is a seeded `CheckThresholds.wordingRunTokens`, not a constant. Proved by reinstating four wrong versions (no carve-out, no content floor, 4-token run, empty posting as clean) — all four caught by named assertions. | — | — |
| D5 | CLOSED | ~~`swap_decision.to_label` is never figure-scanned~~ **CLOSED.** The R3 block now scans swap labels that are NOT already present in the rendered text, so `Org Scaling 60+` / `P&L $18M` sitting in a recorded-but-unrendered swap is caught, and a label already rendered is not reported twice under two names — the naive version of this fix inflates the offender count of a check that names people. Both halves proved by reinstating their own defect. **Touches `checks.ts`**, which this lane's brief did not assign; it is not another lane's file either, and D5 has no other home because the gap is in the caller, not in `figureEcho`. Flagged rather than assumed. | — | — |
| D6 | CLOSED | ~~Extraction gaps a verifier measured~~ **CLOSED, AND THE ROW WAS PARTLY WRONG.** `thirteen`..`nineteen` were **already present** in `SPELLED` on `main` and always were — that claim was stale, and is now pinned by a test so it cannot be "fixed" a second time. The other two were real, and measuring them found something the row did not claim: **two live FALSE ACCUSATIONS**. `extractFigures` was not merely missing magnitudes, it mis-read them — the bare-count scanner took the digits, dropped the magnitude, and the magnitude token became the "unit" the noun rule compares. Measured on `main` at `8e4c46c`: `'Grew the community to 18 million users.'` and `'You will own an 18 million dollar budget.'` BOTH yielded `{raw:'18', key:'num:18', unit:'million'}` → **ECHO**, so a community of users was accused of being lifted from a budget line; `400k users` vs `400k budget` did the same, and the uppercase form (`400K`) was dropped as a proper noun and matched nothing at all, so the verdict flipped on the case of one letter. Fixed by ISO currency codes (`USD 18M` used to yield the figure EIGHTEEN) plus a bare-magnitude scanner keyed in the same `cur:` magnitude space — so deleting a dollar sign no longer launders the posting's figure — and NEVER marked, so an unmarked magnitude must still clear the noun rule. | 300 tests passed and every AC-named form was caught. | — |

## X2 / regen reachability (`claude/qc-regen-reachability`)

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D7 | CLOSED | ~~No independent verifier~~ **DONE.** It found six defects in the guard itself, all fixed: a live false negative (`apply`), a surviving global-name fallback, six missed grammars, a cry-wolf on query-string routes, a staleness floor that could not detect staleness, and the Rebuild affordance having zero test coverage. | — | — |
| D20 | OPEN | **A bare truthy body read (`if (body?.x)`) is out of H33's scope.** `appFacts.ts` reads `body.confirm` that way and is invisible to the guard. | H33 is green and its band is tight, so it reads as complete coverage. | Any future toggle written in the truthy form. Deliberate: including it accused three string-presence checks (`alertText`, `imageB64`, `demoState`) of being unreachable toggles `check: grep api/src/functions/tests/appFacts.ts body\.confirm` |
| D8 | CLOSED | ~~Structural guard not written~~ **DONE** — H34 (`a38c94f`). Took four wrong versions: vacuous, cried wolf, missed a dispatcher, too permissive again. Found a real fourth instance (`ats_source.enabled`), now wired. | — | — |
| D9 | CLOSED | ~~No live verification~~ **DONE, proven live.** Identical requests differing only by `regen`: `{}` → **0** model calls (cache served); `{"regen":true}` → **3** (`:resume` 9,585 tok, `:portfolio` 11,522, `:ats-qc` 6,562). Precondition checked first (`jd_grounded=t`, `has_pkg=t`) so `staleUngrounded` could not mask it. **The recommended evidence was wrong**: `packet.updated_at` moved on the CONTROL too (00:44:30 → 16:41:35) while the content stayed byte-identical, so it is not a regeneration signal — only `usage_metering` is, because a cache hit cannot make a model call. | — | — |

## Cross-lane

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D34 | OPEN | **THE LANES SHARE ONE WORKING DIRECTORY AND ONE `.git`, AND `git stash` IS REPOSITORY-GLOBAL.** `/home/user/boost-application-packet-platform` is checked out by several lanes at once — its reflog shows `qc-r3-gaps` -> `qc-p8-4-follow` -> `qc-contrast-sweep` -> `qc-ledger-live` inside a few minutes, each lane checking out its own branch in the same tree. Consequences, all observed in one session, not theorised: (a) a `git stash` in that tree captures **whatever another lane had uncommitted at that instant** — mine swept up the P8.4 lane's deliberately-reinstated `comparison_dimensionXX` defect mid-verification; (b) `git stash pop` pops **the top of a shared stack**, so it restored a DIFFERENT lane's work into my worktree while my own stash went elsewhere; (c) `git checkout`/`reset` by any lane silently moves HEAD and the tree under every other lane; (d) the local Postgres is shared on one fixed path and port, so `dimensionsDb`'s "the table must not already exist, or this proves nothing" precondition fails whenever another lane has created it. **Work in a `git worktree` (the house pattern — see `/tmp/p83`, `/tmp/p84`, `/tmp/p87`) and NEVER use `git stash` in this repo.** To park a change, copy the file or write a patch. Nothing was lost here only because the affected work had already been pushed. | Every lane's tests pass in isolation, and a rewind or a swept-away edit reads as "I must have imagined making that change". | Now, for every concurrent lane. `check: manual lane — a shared checkout is an environment fact no source grep can see` |
| D10 | CLOSED | ~~H-case ID collisions~~ **CLOSED STRUCTURALLY.** The counter is retired: H1-H44 frozen, and `H26` now FAILS the suite on any new numeric id. New cases take a two-word slug. Four earlier fixes (one id per lane, ranges per lane, claim-at-merge, a same-file duplicate check) all failed because the collision only exists in the union of branches that cannot see each other. | — | `check: grep api/test/hardening.test.mjs test\('H26:` |
| D11 | OPEN | ~~P7 items 4, 6, 8~~ **ITEM 8 DONE; ITEM 4 GROUND-TRUTHED, REMEDY DEFERRED TO THE OWNER (see D29); item 6 was already done.** Item 8: `CONFIG_KEYS` extended with the six RowKeys the Auth & Config screen has been writing all along (`google.resumeTemplateId` / `portfolioTemplateId` / `coverLetterTemplateId` / `outputFolderId`, `microsoft.senderEmail` / `recipientEmail`) — they were written by the console and read by NOTHING, which is worse than not existing because it tells the owner they are in control when they are not. `pipeline.ts`'s byte-identical second copy of the four Drive id literals is gone; `packetTemplates.ts` is the one home for the seeds. `renderArtifact` (the path that actually ships) resolves its ids the same way. Guarded by `H:owner-config-is-read` (behavioural: six DISTINCT owner values through `settingsFromConfig`) and `H:no-second-id-copy`. **NOT verified live** — the sandbox cannot reach the Function App. | — | `check: manual api-test.yml — item 4's remedy is an owner decision (see D29); the P7 config fix is not verified live` |
| D12 | CLOSED | ~~200 with `pass:false`~~ **CLOSED — the 502 route half is now deployed and proven live.** Identical request to the one that used to return 200: `POST /api/pipeline/run {"jobId":"no-such-job"}`, run **32451438687** on `main` at `706151e`, returned **HTTP 502** with `{"pass":false,"outcome":"error","steps":[]}` and the workflow exited 1. Both halves of the split are now live: the CATCH exit returns 502, and `api-test.yml` fails on a `pass:false` body. | — | — |
| D13 | OPEN | **OPEN — the per-rebuild superseded file is UNCHANGED and still leaks, see D31.** The failed-build case is done. `deleteDriveFile` is now the only Drive DELETE in `api/src` (there were zero). `copyThen` owns copy-then-work so a post-copy failure can delete the file whose id previously existed only in the frame that threw — the half a naive `allSettled` still loses. `buildAllOrCleanUp` uses `allSettled` because `Promise.all` rejects on the FIRST failure while siblings run on, which is what made the ids unenumerable at all. Applied to BOTH `pipeline.copyAndInject` and `appPackets.renderArtifact`. Guards `H:orphan-drive-files` (a job that fails FAST while siblings resolve LATE) and `H:orphan-after-copy` (stubbed fetch, asserts the DELETE was issued and the original error survived). **NOT verified live** — no Drive call has been made from this branch. | — | `check: manual api-test.yml — no Drive call has been made from this branch` |
| D14 | OPEN | **`covered_kw` does not mean covered.** The prompt asks for "ATS keywords for this role" with no candidate comparison, and the array renders as green "N covered" chips. **The relabel LANDED** (PR #26, `3153f1a`, deployed green): `NOT_COMPARED_NOTE` and the `KEYWORD_GROUPS.profileCompared` flag now carry the meaning, guarded by `app/test/postingAnalysis.test.mjs`. It stays OPEN because the live half is unconfirmed. | It renders as a coverage number, in green. | P8 close-out. **A live check was attempted and was INCONCLUSIVE — read it as neither a pass nor a regression, and do not repeat it unchanged.** `ui-verify.yml` run **32477734266** on `#/packet/9f9c370a…/jd` reported both expects missing with `consoleErrors: []`. Both strings are CONDITIONALLY rendered — `NOT_COMPARED_NOTE` appears only when an uncompared keyword group is on screen — so their absence measures that packet's data, not the relabel. A real live check needs an expect that renders unconditionally on the JD step, or a `data-qc-group` selector. `check: manual owner — the defect is what `covered_kw` MEANS, which no source grep can settle` |
| D15 | CLOSED | ~~Stale label~~ **DONE.** `assetGate.js` now reads "Must-haves your profile can evidence" and the score card "Must-haves evidenced". A label describing the previous definition next to a correct number is the half a reader believes. | — | — |
| D16 | CLOSED | ~~`engineJudged` counted every must-have~~ **CLOSED.** `must_have_coverage` publishes `judged` (exactly `coverable`); `artifact_score.judged_requirement_ids` stores it; `appChecks` writes what the check published and is source-pinned against recomputing. Column confirmed present in production. | — | `check: grep api/src/functions/tests/artifactScore.ts judged_requirement_ids` |

## P8.4 / comparison dimensions (`claude/qc-p8-4-dimensions`)

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D21 | CLOSED | ~~`comparison_dimension` is NOT in `SCHEMA_SQL` or `EXPECTED_TABLES`~~ **DONE.** Registered in all three places, the third being the one the row said gets forgotten: `SCHEMA_SQL`, `EXPECTED_TABLES`, and H11's hand-maintained array. Both halves of H11 proved by reinstating the defect — dropping the name fails on "not in EXPECTED_TABLES", renaming the CREATE fails on "not in SCHEMA_SQL". **The registration leaves TWO copies of one CREATE TABLE**, which is the shape that diverges in silence: every database built by the ensure-path already HAS the table, so SCHEMA_SQL's `create table if not exists` is skipped there and a CHECK in one copy only would be enforced on fresh installs and absent on production forever. `H:dimension-ddl-parity` (`dimensionsDb.test.mjs`) therefore builds the table BOTH ways against a real cluster and diffs every column, constraint and index — and it immediately earned its keep by catching a CHECK genuinely missing from the SCHEMA_SQL copy in the first commit. `schemaParity.test.mjs` green (2/2, not skipped). **NOT verified live**: `diag/pg-migrate` has not been called, so that the completeness report now lists the table is inference, not observation. | — | `check: grep api/src/functions/tests/schema.ts comparison_dimension` |
| D22 | CLOSED | ~~`experience.years_leadership` unreachable in `checkAgainstFacts`~~ **FIXED** on `claude/qc-live-defects`. Selection is no longer by catalogue POSITION: `FactDef.refines` declares that `years_leadership` narrows `years_total`, and `selectFactDef` drops any matching def that another matching def refines. The gate change is the one D22 predicted and it is the intended one — a leadership requirement answered only by a total-years figure is now `unknown` (which PROPOSES the leadership fact) instead of falsely `satisfied`, and a recorded-but-short leadership figure WARNS with its own arithmetic. **`coverable` membership is unchanged** — `ownedByFacts` already absorbed both `unknown` and resolved verdicts, so the same rows leave `coverable` either way; what changed is WHICH fact the verdict is about. `H41` was rewritten from pinning the shadow to guarding the fix (undeclared strict-subset relations measured over a 25-line corpus, plus the behavioural half), `H41b` and `H43` added, and `checks.test.mjs`'s fixture — which asked for LEADERSHIP years, recorded only TOTAL years and asserted a pass — corrected. **NOT verified live**: no before/after count on real data, because the sandbox cannot reach the database. | — | — |
| D23 | CLOSED | ~~Org size and budget are never graded from their numbers~~ **DONE.** `parseQuantity` in `ownerFacts.ts` is the ONE demand parser, extended per unit (years byte-for-byte unchanged; people widened past the two-digit cap that read "1,200" as 1; usd returning dollars). `H:one-demand-parser` fails if a second extractor appears in `dimensions.ts`. **It uncovered a live defect bigger than the row:** Settings ▸ Facts (`Settings.jsx:1489`) saves `Number(String(v).replace(/[^0-9.]/g,''))`, so a typed "$18M" is stored `value_num: 18`, while `deriveFacts` stores the same figure as `18000000` — one column, two writers, six orders of magnitude apart, both live in `owner_fact` today. Comparing `value_num` naively prints "Falls short" at an owner who runs an $18M budget, so for usd the magnitude is re-read from the fact's TEXT; nothing is rescaled upward ($18K still falls short of $10M) and a figure whose scale nothing states is refused. `H:usd-scale-parity` and `H:comparator-units-agree` guard both halves; the half-fix (widening the predicate but not the arithmetic) fails 7 named tests. `H:facts-widen-no-coverable-shift` MEASURES the gate blast radius rather than predicting it: the coverage population is byte-identical while `facts_needed` shrinks. `DIMENSION_VERSION` 1→2. **NOT verified live**, and see D23b. | — | — |
| D23b | OPEN | **`owner_fact.value_num` is written on two different scales and the STORAGE is not fixed.** Settings ▸ Facts stores a typed "$18M" as `18`; `deriveFacts` stores the same figure as `18000000`; `upsertStated` (`appFacts.ts`) takes the client's `valueNum` verbatim, so both shapes are in the column today. D23 made the READERS safe — for usd the magnitude is re-read from the fact's text — but a bare figure with no magnitude anywhere is still ungradeable, and anything else that reads `value_num` for a money fact still reads whichever scale that row happens to carry. The honest fix is one scale at the WRITER plus a backfill, which changes stored data and needs an explicit migration and a `db-query.yml` audit; it also touches `appFacts.ts` and `Settings.jsx`, neither of which this lane owns. **The size of the problem in production is unknown** — nobody has run `select key, value, value_num from owner_fact where key in ('scope.largest_team','scope.largest_budget')`. | The readers are correct now and the JD step grades money properly, so nothing looks broken. `H:usd-scale-parity` is green — it guards the comparison, not the column. | The `appFacts.ts` / `app/` lane, or any lane running `db-query.yml`. Run the audit query first: it is one workflow dispatch and it sizes the work. `check: manual db-query.yml — the audit query sizes the affected rows` |
| D24 | CLOSED | ~~NOT DONE, and now CONFIRMED LIVE — the product tells the owner to change a setting on a screen that does not exist~~ **CLOSED — the screen exists and renders on production.** `Settings ▸ Quality` now carries a Comparison dimensions card: role-family picker, the catalogue rendered from the API's own list, and a save through the new `api.dimensionPrefsSet`. The API half had been live and UNCALLED — `grep -ic dimension Settings.jsx` was 0 while an evidence run returned *"used the seeded default set — change it in Settings ▸ Comparison dimensions"*, naming a page that did not exist. **VERIFIED LIVE** (deploy `c230f30`, ui-verify run **32542401407**): `missingExpect: []`, `consoleErrors: []` against `#/settings/quality` for the real owner. | — | — |
| D25 | CLOSED | ~~Nothing about P8.4 is verified live~~ **CLOSED — P8.4 built on production data.** `POST /api/app/opportunity/9f9c370a.../evidence`, run **32451913037**, returned `comparison: {rows: 8, graded: 1, family: "technology"}` and `db-query.yml` run **32451983590** confirms `comparison_dimension` holds **8 rows**. The engine, the store and the grading all run against real data. What the run ALSO exposed is filed separately: `D:evidence-resolves-nothing` and `D24`. | — | — |

## Live but unconfirmed

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D17 | CLOSED | ~~Nothing deployed~~ **CLOSED — every phase lane is on `main`.** The row last read "P3 is the only lane still unlanded" and that had stopped being true: `claude/qc-p3-remediation` is an ancestor of `main`, and `git branch -r --merged origin/main` lists 64 `claude/qc-*` lanes covering P0-P8 and every X prerequisite. What remains is not landing but LIVE VERIFICATION, which is D25, D37, D12, D13 and D11 — separate rows, each naming its own vehicle. | The row named a specific lane, which reads as current until someone checks the ref. Its check was `manual lane`, so nothing re-checked it — a reminder that a manual row is unverified, not safe. | — |
| D18 | CLOSED | ~~P0.3 dark accent pills at 1.90:1~~ **CLOSED — the claim does not reproduce.** Re-measured `.px-btn-accent` and `.px-btn-dark` with `getComputedStyle` in Chromium on `main` at `ee3d989`: **9.05:1 light, 5.94:1 dark**. Both pass. The buttons take `--surface-brand-default` with `--text-on-brand` on top, not `--proto-accent`, so the token this row named was never the one they read. Either P8.7 fixed it or the original 1.90:1 was measured against something else — I did not establish which, and the row is closed on the measurement, not on the history. **The real accent defect was a different one and is now fixed** (see D26). | The row named a plausible token and a specific ratio, and neither was checked again for two phases. | — |
| D19 | CLOSED | ~~Stored evidence is never re-validated on read~~ **FIXED** on `claude/qc-d19-evidence`. `requirementsGet` now re-reads the profile (`sourceText()`, one Docs + one Table read per GET — the cost is accepted deliberately: the owner edits their profile outside this API, so there is no cheaper ground truth than the record itself) and every stored row goes through `verifyEvidence`. **The decision is REFUSE, DO NOT GUESS** — the same discipline `correction.revertOne` already applies to `before_sha256`: a rotted excerpt is WITHHELD rather than caveated, and it is not re-resolved on the read path, because re-resolving is a write on a route readable with an unverified `?owner=`, and re-ranking belongs behind `requireWrite` on `POST /evidence`. **The offsets are the claim, the digest is only the alarm**: a record edited elsewhere whose offsets still yield the quote byte-for-byte stays proof (`recordChanged: true`), so an over-strict fix cannot empty the JD step on any profile edit. Five states, each with its own sentence and its own health bucket: `verified` / `stale` / `misresolved` / `source_missing` / `unverified` / `none`. **`misresolved` is the H32 residue detector** — record byte-identical AND offsets still wrong means they were wrong when WRITTEN (the pre-`EXTRACTOR_VERSION 2` lower-cased-copy bug), and telling that owner "your profile changed" would be a false statement about them. 15 new tests; seven reinstated defects each fail a named assertion. **NOT verified live** — see D36. | The excerpt renders normally and is a true substring of what the record USED to say. | — |
| D36 | OPEN | **The comparison card still serves a COPY of the evidence quote and it is not re-validated.** `comparison_dimension.profile_value` is written by `buildComparison` from the evidence quote at build time, and `requirementsGet` serves `comparisonPayload` — the STORED rows — not a rebuild. So D19's fix protects the requirement list and NOT the JD step's comparison card, which is the surface an owner actually reads: after a profile edit it keeps printing the old excerpt until someone runs `POST /evidence` or the backfill. `rebuildComparison` now verifies before grading, so every rebuild is clean; nothing rebuilds on read. The honest fix is either re-validating the stored `profile_value` in `comparisonPayload` or dropping the copy and joining live — both in `appDimensions.ts`, which the D19 lane does not own, so it stopped rather than bolting a redaction onto the read path. | The card renders a quote, the requirement list beside it is now correct, and `comparison.stale` is green because that flag means a dimension-set mismatch, not a stale quote. | The `appDimensions.ts` lane. One decision: re-validate the copy, or stop copying. `check: grep api/src/functions/tests/appDimensions.ts profile: r\.profile_value` |
| D37 | CLOSED | ~~The number of affected rows in production is unknown~~ **CLOSED — the count is zero, and D19 is live.** `db-query.yml` run **32451983590**: `requirement_evidence` = **0 rows**. D19's re-validation is running — run **32451913037** returned a populated `evidenceHealth` block (`verified:0, stale:0, misresolved:0, sourceMissing:0, none:10, profileReadable:true, verifyVersion:1`). There is nothing stored to go stale because nothing is stored at all, which is `D:evidence-resolves-nothing`, not this row. | — | — |
| D:remediation-atomicity | OPEN | **A remediation pass's package mutation is not atomic with its ledger row.** The ledger inserts and escalations now share one transaction (all-or-nothing), but `packet.pkg_json` is updated per pass BEFORE the ledger is written, so a failure between them leaves a package this run modified with no row recording it. The handler now REPORTS that state explicitly (`packageMutated: true, ledgerWritten: false`) instead of returning a bare 500 — reporting it is not fixing it. | The endpoint returns a structured 500 that reads like a handled error, and re-running is safe, so the window is easy to mistake for closed. | `evaluateArtifact` (P8.3's file) giving up ownership of its own `begin`/`commit`, which is what makes a transaction spanning a whole pass possible `check: manual lane — statement ORDER across two functions is not a grep` |
| D:escalation-term-target | OPEN | **`escalation.ats_term_id` has no foreign key and no writer.** The column exists and the `check (requirement_id is not null or ats_term_id is not null)` names it, so it reads as a supported second target. Every escalation this lane writes sets `requirement_id`. | The column is in the schema and in a CHECK. | P1.2 publishing a `term_library` version worth pointing at `check: absent api/src/functions/tests/appRemediation.ts ats_term_id` |
| D:pass-ceiling-contradiction | OPEN | **P3-09 and P3-33 are contradictory and neither acceptance document reconciles them.** P3-09 says no `remediation_loop` row may have `n > max_passes` for a packet; P3-33 requires a resolved escalation to continue the ledger at `max(n)+1`. The implementation follows P3-33, so a second run on a 4-pass ceiling writes n=5..8. This is an unresolved criterion conflict, not an implementation slip. | Both criteria are marked sandbox-verifiable, and the loop passes every test written for it. | an owner decision on whether the ceiling is per-run or per-packet `check: manual owner — whether the pass ceiling is per-run or per-packet` |

## Contrast

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D26 | CLOSED | ~~Eight of nine status tones below 4.5:1~~ **CLOSED, fixed and deployed** on `main` at `ee3d989`. Seven token values; `accent` was 2.90:1 in dark — the worst text in the app, labelling the QC rail's swap decisions. `app/test/browser/run-tones.mjs` now sweeps all nine tones in both themes, reading `TONE` from `shell.jsx` rather than a copy, and fails loudly rather than silently when blinded. | — | — |
| D27 | CLOSED | ~~`px-small` tertiary ink at 2.56:1~~ **CLOSED.** `--proto-ink3` moved to `--neutral-500`. Measured before: 2.43 / 2.56 / 2.43 / 2.31 light, 3.07 / 3.75 / 3.07 / 2.18 dark — it failed on EVERY ground in BOTH themes, so the token's value was wrong, not one class's use of it. Documented trade: tertiary and secondary ink are now the same colour until the ramp gains a step between 400 and 500. | — | — |
| D28 | CLOSED | ~~Nothing sweeps contrast outside the nine tones~~ **CLOSED.** `run-contrast.mjs` measures every colour the app paints against every ground it paints it on, both themes: **1,062 checks, 0 failures** (was 38 checks / 9 tones). Found 101 real failures; all fixed. The registry is curated — every colour carries a reason, decorative fills carry an explicit `skip`, and a STALE entry fails the suite. It caught two of my own fixes. | — | — |

## Ledger and guards

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D:evidence-resolves-nothing | CLOSED | ~~The evidence spine evidences NOTHING on real production data~~ **CLOSED — 1 -> 6 evidenced on the live Trinnex posting, and the remaining 4 are correct refusals.** The row closed in two halves. QUOTE QUALITY (2026-08-21): bullet sub-runs took the stored excerpt from 286 to 130 characters with the ratio unchanged at 0.667 — owner-settable as `chk_evidence_bullet_run`, where lower is broader. REACH: the token matcher provably could not close the gap (`improve operational reliability` shares no content word with `reduced outages from nine hours to one`), so the escalation tier was built — a model proposes, `verifyProposal` accepts only a byte-exact `indexOf` substring of the record it names. **MEASURED LIVE** (deploy `444e436`, api-test **32541296898**, db-query **32541330806** and **32541365164**): `escalated 8, proposed 5, model_declined 3, not_worth_escalating 1, misresolved 0`. **THE QUOTES WERE READ, AND THEN THE READING WAS CORRECTED.** All five are good matches: "high-performing engineering culture" -> "I have fostered high-performing teams"; the software-strategy row -> the 3-year roadmap and $13M budget; engineering practices -> the enterprise Agile transformation; "scalable, secure, high-quality software" -> redesigning a service into a scalable digital experience with real-time decision support; "AI/ML and IoT" -> a SaaS platform integrating real-time data collection, data modeling and predictive analytics. **THREE OF FIVE MATCHES ARE CLEAN; TWO ARE PARTIAL AND THEIR JUSTIFICATIONS FABRICATE THE MISSING PART.** Settled against the PRIMARY SOURCE — the posting — not against the quote or the reasoning (db-query **32542977438**): seq 2's `verbatim` is `scalable, secure, high-quality software` and seq 8's is `IoT data, models, geospatial data, and AI/ML`, both `match_method='anchored'`, both terms present in `jd_text`. **The extraction is faithful**, so the requirements genuinely demand security and IoT. The profile quotes evidence the other components (scalable, high-quality; data modeling, predictive analytics) and NOT those two — and the stored justifications assert exactly the missing component: *"...enhance quality and security through real-time decision support"* from a quote mentioning no security, and *"experience with IoT data"* from one saying "real-time data collection". The excerpt is verbatim; the JUSTIFICATION closes a gap the evidence does not. Filed as `D:proposal-reasoning-unverified`. **This row briefly said the opposite** — I retracted the finding under push-back before checking the posting, which is logged in `.claude/accuracy-log.md`; the retraction was the error, not the finding. Escalation is ON by default per the owner. | — | — |
| D:schema-not-migrated | CLOSED | ~~A table registered in `SCHEMA_SQL` is not thereby IN production~~ **CLOSED — the deploy now applies the schema and fails loudly if it did not.** `api-deploy.yml` gained a final step that waits for the worker to converge (polling `/api/health` rather than guessing a sleep), POSTs `/api/diag/pg-migrate`, and reads the JSON **`ok` field, not the status code** — `pgMigrate.ts:48` returns HTTP 200 with `{ok:false, error}`, the identical shape that let a failed pipeline run report success until `D12` fixed the caller, so reading the status here would have rebuilt that defect one workflow over. Proven against four bodies before landing: the real success shape exits 0; `ok:false` carrying the actual `relation "comparison_dimension" does not exist` error exits 1; a missing `ok` exits 1; a non-JSON gateway page exits 1. | — | — |
| D:remediation-never-ran | OPEN | **IT HAS A CALLER NOW — EXECUTION IS STILL UNOBSERVED, AND THAT IS WHY THE ROW STAYS OPEN.** The original finding was two things wearing one name: P3 was deployed AND nothing in `app/` invoked it. The second half is fixed (2026-08-22, deploy `a02a85c`): `api.js` gained `artifactRemediationGet` / `artifactRemediate` / `escalationResolve`, `useQcEntries` fetches the ledger on the QC step, and the tab reads real passes instead of falling back to `insertion.loop` while telling the owner the controller did not exist. `H:remediation-has-a-caller` fails if the wiring is removed. **What this row now measures is the ORIGINAL claim, unchanged: `remediation_loop` has 0 rows in production.** A caller that exists is not a pass that ran — that is the same conflation this row was opened to name — so it closes on the count, not on the wiring. | The tab now renders the real ledger shape and the routes respond, so the feature looks operational; an empty ledger reads as "no passes needed yet", which is exactly what it read as before. | A build whose gate does not pass, which is what triggers a remediation pass. `check: manual db-query.yml — select count(*) from remediation_loop must exceed 0` |
| D:build-runs-no-qc | CLOSED | ~~The evidence resolver is called by NOTHING in the product~~ **CLOSED ON THE BUILD PATH THIS TIME.** It was closed once on the wrong evidence — an `api-test` dispatch against `POST /evidence` directly, which mints a session token, while the build path itself used `selfPost` (no Authorization header) against a route that calls `requireWrite`. Every build logged *"evidence resolve did not run: sign in required to modify this workspace"*; the pass had never executed there. Fixed by calling `resolveEvidenceForOpp` IN-PROCESS — `packetBuildAll` already holds an authenticated client and the resolved owner, so there is no HTTP hop, no credential to mint, and nothing added to the gateway budget D35 is losing. **MEASURED ON A REAL BUILD** (deploy `51c65e2`, api-test run **32547465648**, opportunity `2cb56fb3`): `evidence: {total: 35, evidenced: 8, proposed: 8, escalated: 12, refused: 0}`. This is the posting that sat at 0-of-38 before the escalation tier — all eight came from escalation, which is the case it was built for. `H:draft-is-written-from-prompts-not-evidence` asserts exactly one evidence pass, after the artifacts, and fails if the unauthenticated route call returns. | — | — |
| D:facts-before-evidence-precedence | OPEN | **Eligibility and quantity requirements are refused by the evidence path and never picked up by the fact path that could answer them.** `Reside in the East Coast of the United States` is correctly refused as `eligibility` — a residence is a fact about a person, not a phrase in a résumé — but the owner HAS a confirmed answer: `owner_fact.identity.location = 'Westminster, MD 21158 (Maryland)'`, `source=owner_stated`, `confirmed_at` set. Twelve such facts exist, all confirmed (location, work authorization, security clearance, highest degree, certifications, years total/leadership, largest team/budget, relocation, travel, work mode). Three of the ten Trinnex requirements are eligibility- or quantity-shaped, so a third of that posting is answerable from data the owner already gave the system. | The requirement shows as unevidenced with a correct-sounding reason, and `checks.ts` excludes eligibility from `coverable` and names it in `elig.offenders`, so the reporting is internally consistent and looks finished. Nothing says 'we hold the answer and did not use it'. | **OWNER PRECEDENCE, STATED 2026-08-21 AND BINDING ON THE FIX: the specific TEMPLATE RESUME is the FIRST source because it may carry per-template tweaks; `owner_fact` is the FALLBACK.** Not the other way round. Settling a location requirement also needs one reasoning step a deterministic rule cannot take (Maryland is on the East Coast) — that is the escalation tier's job, and it carries no fabrication risk because it reasons over OWNER-STATED, CONFIRMED testimony rather than inferring from prose. `check: absent api/src/functions/tests/checks.ts identity\.location` |
| D:locate-truncates-requirements | OPEN | **`locate()` stores a truncated requirement, and the effect is NOT where it looks.** Measured on opp 9f9c370a: the model's paraphrase is a complete sentence and `verbatim` holds a noun phrase — `Build and promote a high-performing engineering culture` (55 chars) stored as `high-performing engineering culture` (35); `Ensure delivery of scalable, secure, and high-quality software` (62) stored as `scalable, secure, high-quality software` (39). `locate()` anchors on the densest noun phrase and drops the verb, the same defect M6 names one layer down. Corpus-wide: 22.1% of the 7,048 located rows start with a lowercase letter and 13.1% are four words or fewer. **A PREMISE WAS FALSIFIED HERE AND THE RESULT IS THE POINT.** The obvious conclusion — that truncation suppresses matching — was tested before shipping and is WRONG: `requirementSupport` already treats verbs as WEAK and excludes them from the denominator, so both renderings carry identical contentful tokens and BOTH resolve (the fragment scores ratio 1.0 against the full sentence's 0.6). A change to resolve against both renderings was written, tested, found to be a silent LOOSENING — it picks whichever rendering is easier to satisfy, `Knowledge of AI/ML and IoT technologies` over the employer's five-item list — and reverted unshipped. | The stored row reads as a requirement, the count of located rows is high (82.8%), and matching is unaffected, so nothing downstream complains. The truncation is invisible in every number the system reports. | **Where it DOES bite:** `checks.covers()` tokenizes `verbatim \|\| item_text` against the GENERATED DOCUMENT, so a truncated requirement makes `evidence_placed` easier to satisfy than the employer's actual sentence would be — a loosening in the placement check, not the evidence check. Also affects what a reader SEES as the requirement. `check: grep api/src/functions/tests/checks.ts itemTokens\(r\.verbatim \|\| r\.item_text\)` |
| D:compound-requirements-unevidenceable | OPEN | **The requirement extractor emits COMPOUND rows that no single verbatim excerpt can evidence, by construction.** Measured on CTO posting `c5671835` (run **32507372338**): 0 of 38 evidenced AFTER the matcher was fixed and while opp `9f9c370a` evidenced 1 of 10 on the same profile in the same deploy. Its rows read `Improve software delivery velocity, quality, reliability, scalability, and overall engineering health` and `Establish architecture direction across Java, microservices, cloud, private cloud, data, APIs` — a dozen contentful tokens each, several of them separate requirements wearing one row. R2 requires the excerpt to be ONE contiguous span of ONE named record, and M2/M15 forbid stitching a quote across sentences or records, so a compound row is unevidenceable no matter how good the matcher is: no résumé sentence contains all twelve. This is the EXTRACTOR's split, upstream in `requirements.buildRequirements`, not the matcher. | The route returns 200, `evidenceHealth` is populated and totals reconcile, and `unevidenced: 38` reads as 'the profile is a poor fit' rather than 'these rows cannot be evidenced at all'. The matcher also refuses them for individually CORRECT reasons — the profile really does not say `onshore` or `Java` — so every refusal looks justified one row at a time. | `check: manual lane — needs a decision on whether buildRequirements should split a conjunctive posting line into separate requirement rows; splitting changes requirement seq numbering, which `artifact_score.uncovered_requirement_seqs` and `parseMustHaveSource` (H44b) both parse, so it is not a local change` |
| D:api-duplicate-keys | OPEN | **Three keys are defined twice in the same `app/src/api.js` object literal** — `artifactChecksResult` (`:142` and `:179`), `artifactInsertions`, and `packetSwaps`. In a JS object literal the later definition silently wins; there is no error and no warning. They look like parallel-lane merge artifacts. Separately `requirements` (`:139`) and `oppRequirements` (`:167`) are two names for a byte-identical call, which is the R4 "one source per number" smell one level up. | Both definitions currently appear equivalent, so nothing misbehaves and the build is green. A future edit to the FIRST definition would be silently discarded, which is the failure mode. | The `app/` lane. Verify the pairs are actually identical before deleting either — assuming equivalence is how the wrong one gets kept. `check: grep app/src/api.js artifactChecksResult` |
| D:appconfig-unreachable-in-product | CLOSED | ~~All ten pipeline settings are unreachable from the PRODUCT~~ **CLOSED — Settings ▸ Quality ▸ Pipeline, and the route it calls had to be SECURED first.** The ten `CONFIG_KEYS` (temperatures, four template ids, output folder, sender, recipient, default role focus) now render from the API's own key list rather than a hand-written form, so the pipeline and the screen cannot drift. Blank means the code seed, which is what a run already warns about (`D32`: *no roleFocus configured ... used the code seed "engineering"*). **THE PREREQUISITE WAS A SECURITY DEFECT, found while wiring it:** `GET/POST /api/config` shipped `authLevel: 'anonymous'` with NO session check on either method, and the GET returned **every row of the AppConfig partition named `auth`** — an unauthenticated enumeration of that partition and an unauthenticated upsert into it. `grep -rn "api/config" app/ web/ scripts/` returned NOTHING, so it had no caller at all, which is both why it went unnoticed and why tightening it broke nobody. Now: `requireWrite` on the mutation, and BOTH methods bounded by the imported `CONFIG_KEYS` whitelist so a credential landing beside the pipeline settings is never served — deny-by-default, not a denylist. `H:config-route-is-not-open` guards all three halves and each fails on reversion. **This was the fourth deployed-but-uncalled subsystem in one build** (D:build-runs-no-qc, D24, D:remediation-never-ran, this) — the shape is now a guard in `H:remediation-has-a-caller`. | — | — |
| D:hslug-scan-one-file | OPEN | **`H26`'s uniqueness scan reads only its own file** (`hardening.test.mjs:788`, `readFileSync(new URL('./hardening.test.mjs'...))`), so an `H:` slug minted in a second `api/test/*.test.mjs` is invisible to it. Nothing mints one today — `deferredLedger.test.mjs` uses `D:` — so this is a latent gap, not a live defect. | `H26` is green and its message says it guards H-case ids, which reads as covering all of them. The blindness is the same shape `H26`'s own comment records as its fourth failed remedy. | The next lane to touch `hardening.test.mjs` — extending the read to the `test/*.test.mjs` glob is a few lines, but that file is contended and a splice there has produced an unparseable file twice. `check: grep api/test/hardening.test.mjs readFileSync\(new URL\('\./hardening\.test\.mjs'` |
| D:id-hygiene-duplicated | OPEN | **`H26` and this guard implement the same id hygiene twice.** `one-id-one-row`, `counter-retired` and `slug-has-two-words` re-implement, structurally line for line, what `H26` does for `H:` ids — including a second frozen-maximum constant (`hardening.test.mjs` = 44, `deferredLedger.test.mjs` = 37). A shared helper taking `(ids, frozenMax, prefix)` was available and not taken. | Both files are green and each guards its own namespace correctly, so nothing looks wrong until the convention changes and only one file is updated. | The same lane that takes `D:hslug-scan-one-file` — both are edits to `hardening.test.mjs`, which is contended and where a splice has produced an unparseable file twice. `check: grep api/test/hardening.test.mjs const FROZEN` |

## Live findings, 2026-08-21 — surfaced by the first corrected build on production

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D29 | OPEN | **R3 is live, has now run on a posting that DOES contain figures, and correctly produced zero.** The earlier zero was explained by the Trinnex posting containing 0 figures. Opportunity `2cb56fb3` contains **$279,000** and **15+**, and `applyCorrectionPass` runs in the build path (`appPackets.ts:381`, with `resolvePostingSource(opp)`), so it executed. `correction` is still **0** — and that is the RIGHT answer: the stored package (8,647 chars, written by that build) does not contain `279,000` or `15+` at all (`db-query` run **32487971188**). The generator never echoed the employer figures, so there was nothing for R3 to correct. | Zero corrections after a build reads as a dormant or broken subsystem. Two runs now show it declining correctly rather than failing. | **R3 has still never been observed FIRING.** It needs a package that actually echoes an employer figure — which may require constructing one deliberately, since two real builds did not. `check: manual db-query.yml — select count(*) from correction` |
| D30 | CLOSED | ~~compact_resume: one red run, one green~~ **CLOSED — the third run is green, on a different posting.** The row would not close on a coin, and it should not have: one red then one green is not evidence. Run **32546312184** (eMoney `2cb56fb3`, `regen:true`) built `compact_resume` at 02:30:10 with a doc_url and 1,725 characters of content, alongside all three siblings. Red, green, green — the prompt fix held across two postings. | — | — |
| D31 | CLOSED | **CLOSED LIVE 2026-08-22 — Call 2 was never going to return JSON, and now it is not asked to.** This row was closed after the `portfolio_user` prompt fix on the strength of one Trinnex build where all four fields populated. A build on opportunity `2cb56fb3` (run **32487630490**) returned: *"resume: Call 2 (portfolio) returned no JSON object (no parseable JSON object in 3966 chars) — portfolio/cover fields fall back to Call 1"*. So the fix held for one posting and not for the next. One green run after one red run is a coin, which is exactly what `D30` says about `compact_resume`. **ROOT CAUSE SETTLED 2026-08-22 by reading the prompt itself, which is the one source that could settle it.** `GET /api/prompts?key=portfolio_user&tail=700` (api-test run 32553002646) returns v002, 7,714 chars, ending: *`### Word and Character Requirements Check ### - Generate a table formatted with html tags` / `return "Removed"`*, and earlier *"Column 2: Insert The full list of Skill1, Skills2, relevant skills 1..."*. **The owner's Call-2 prompt asks for `### sections ###` and HTML tables. It never asks for JSON.** So `parseAgentJson(r2)` cannot succeed and never could — this is not a flaky model, a prompt bug or a duplicate-prompt problem (the duplicate-prompt guard did NOT fire in the 2026-08-22 build). It is the CODE holding the wrong expectation about the owner's prompt. Measured cost, four times in one build (job `945e28ed`): 2,957 / 3,178 / 4,736 / 5,404 characters of Call-2 output discarded, with portfolio and cover fields silently falling back to Call 1 — so the dedicated portfolio call has never once contributed to a document. **FIXED AND CONFIRMED LIVE (`4fb00e1`, api-deploy run 32553575827; build job `1c0ad2f3`, `state: done`, 198s).** Call 2 is parsed with `parseResumePackage`, and its refined lists rank between Call 3 and Call 1 — the QC pass still wins because it runs last and now receives the refinement as its input; Call 1 remains the floor. Measured on that build: **`Call 2 (portfolio) returned no JSON object` warnings 4 → 0**, ten Call-2 sections now parsed and their genuinely unmapped analysis sections reported instead of the whole reply being discarded, and the document changed — `SkillsBullets1` 232 → **385**, `SkillsBullets2` 153 → **239**, `RelevantBullets1/2/3` 89/94/89 → **65/60/63**. The direction of both moves matches the prompt's own hard requirements (10-11 skills per list; each Relevant list ≤3 items with at most one over 20 characters), which is the refinement running for the first time. **The `assemblePackage` expectation that Call 2 supplies `coverLetter`/`aboutMe1`/`aboutMe2`/`executiveProfile`/`coldEmail` was fiction** — node 299599701 emits none of them; in the zap they come from the baseline `set_value` nodes and Call 1. No prompt was edited. Guards: `api/test/callTwoRefinement.test.mjs`, five cases, three mutations proven to bite.  | The packet still builds — Call 1 is a working fallback, so `built: 4, failed: 0` and the documents exist. The failure is one line in a ten-line warning list. | Needs a third and fourth build to size how often it fails, before any prompt change is justified. `check: manual api-test.yml — build again and count Call 2 failures across postings` |
| D32 | CLOSED | **CLOSED 2026-08-22 — the resume TEMPLATE now decides the focus, and the value is set.** The owner's ruling: *"let the resume chosen drive the persona, right now it's only engineering available"*. **Why the old path could never have worked:** the first source looked up `templates/<roleRowKey(roleType)>`, and `roleType` is the POSTING'S FREE-TEXT JOB TITLE — literally `templates/director-of-digital-technology-operations-&-innovation`. No such row exists for any real posting, so that source was dead on arrival; the `persona` source below it is dead too (`opportunity.persona_key` NULL on **1,676 of 1,903** rows, design abandoned, confirmed by the owner); so every build fell through five layers to a code constant and wrote an executive Director of Digital posting for *"a senior ENGINEERING executive"*. Setting `openai.defaultRoleFocus` would have papered over that with one global value for every posting. **Now:** `templates/resume-<driveId>` is read first, keyed by the Drive id the build actually copies (`CONFIG_KEYS.resumeTemplateId`), so it cannot drift from the document being written. Only one template exists, so NO document changes — `source` moves `seed` → `template`, which is the entire point. Writer shipped with it (`GET`/`POST /api/config/templates` + Settings ▸ Quality ▸ Resume template focus); blank CLEARS rather than storing `''`, which would beat the seed and blank the directive every prompt is prefixed with. **Verified live (deploy `ec2ecbe`):** unauthenticated POST → **403** (run 32558336819), authenticated POST → 200 (run 32558355526), GET now reports `roleFocus: engineering, source: config` (run 32558373038). | The focus is stated by the resume being built instead of reached by exhausting every other source. | — |
| D33 | OPEN | **Generated sections that map to no merge field are discarded on every build — and it is BIGGER than this row recorded.** A full build on a second posting (run **32487630490**, 4/4 artifacts, `failed: 0`) discarded **8 sections totalling 7,446 characters**: `Job Description Summary` (3,482), `Missing ATS Swap Suggestions` (1,511), `Missing ATS Skills` (1,306), `Skills1` (399), `Skills2` (358), and `Relevant Skills bullet list 1/2/3` (126/134/130). The last five are not dead prompt cruft — they are SKILLS CONTENT that never reached the resume, which is a different and worse problem than the four prompt sections this row was opened for. **RE-MEASURED AND RECLASSIFIED 2026-08-22 (job `945e28ed`, 42 warnings — but see the correction below: that 42 was ONE generation's 10 counted four times, because a rebuild then ran the pipeline once per artifact; fixed in `67e4caa` and the per-build figure is now 10, confirmed on job `3ae8d684`).** The discarded titles are `Missing ATS Swap Suggestions` (354/472/610/828), `Second Job Description Check` (2,694), `Skills1` (315-357), `Skills2` (320-364), `Relevant Skills 1/2/3` (132/138/140), `Relevant Skills bullet list 1/2/3` (103-127), `Job Description Summary`, `Missing ATS Skills`, `Jobscan Extraction`, `Word and Character Requirements Check` (7 chars — literally `Removed`, exactly what `portfolio_user` v002's last line tells the model to return). **THE DRAFT IS NOT LOSING CONTENT, and that changes what this row is.** Ground truth: `packet.pkg_json` for that build (8,065 chars) has EVERY draft field populated — `SkillsBullets1` 232, `SkillsBullets2` 153, `RelevantBullets1/2/3` 89/94/89, `ResumeSummary` 381, `ExpertiseBullets` 260, `WorkHistoryBullets1-4` 671/565/554/674, `@CoverLetterBody` 1,521, `@ExecutiveProfile` 457, `@AboutMe1/2` 413/458, `@CoreAccomplishments` 788. The unmapped `Skills1`/`Relevant Skills N` sections are SECOND occurrences: `resumeParser.ts:155` is first-unfilled-wins, and the owner's prompt asks the model to restate those lists inside the swap table *"before any swaps"*. The placed field is the post-swap final; the discarded copy is the pre-swap original plus the swap reasoning. **So what is being thrown away is the ATS/QC ANALYSIS the owner's prompt explicitly asks for — which swaps were made and why — not draft text. That is exactly the evidence this layer exists to keep.** Fix direction: capture unmapped sections rather than warning and dropping them; placement in the document is NOT wanted for the analysis titles.  | The build reports `built: 4, failed: 0` and returns four real Google URLs. The warnings are a long list below a success. | **Owner decision stands: the prompt stays as it is until the current one is proven working** (`docs/qc-evidence/P7-ACCEPTANCE.md`). But the `Skills*` half was never part of that decision and should be re-scoped separately. `check: grep api/src/functions/tests/pipeline.ts maps to no merge field` |
| D:route-registered-twice-is-a-404 | CLOSED | **TWO `app.http` REGISTRATIONS ON ONE ROUTE MEANS THE SECOND IS A SILENT 404, and three routes had it.** Found by deploying a new writer that did not answer. Measured, not reasoned: `GET /api/config` → 200 (run 32558078459), `POST /api/config` → **404** (run 32558143290) — same route, two registrations, only the first exists. So `saveConfig` had never been reachable on any day of its life and **the Settings ▸ Pipeline Save button could not have worked**; `app/coach/config` carried the identical pair so the coach settings could not be saved either; and `config/templates` was written the same way and inherited it within the hour. Nothing catches this — the code compiles, the deploy succeeds, the host registers both names without complaint, and the only symptom is a 404 on a route the source plainly defines, which reads like a converge delay. All three are now one function per route dispatching on `req.method` (the shape `promptsApi` already used). `H:one-http-registration-per-route` walks every `app.http` in `api/src/functions` and fails on a duplicate; reinstating the split makes it fail. **Correction on the record:** I reported `POST /api/config` as writable unauthenticated. The `requireWrite` weakness was real in the SOURCE, but the route was a 404, so it was never exploitable. | Two Save buttons that could never save, and no error anyone would read as a routing bug. | — |
| D:swap-screen-reads-a-dead-pass | OPEN | **THE SWAP ROWS NAME THE WRONG PASS, and say so in words.** *(Row re-stated 2026-08-22 after an independent AC read corrected my mechanism — the original wording said the screen compares against a dead pass, and that is NOT what is wrong.)* `swaps.ts:175` takes the after side from `pkg[f.merge]` — the SHIPPED list — with `call3[f.passB]` only as a fallback, so **the comparison target is already correct**. Three things are not: **(1) provenance** — `swaps.ts:185` labels every final item Call 1 did not produce as `originOf(fin, 'pass_b')`, and `pass_b` means Call 3, so every Call-2 insertion is recorded as the ATS pass's work; **(2) the stored rationale is a false statement** — `swaps.ts:205` writes *'reworded by the ATS pass'* and `:234` writes *'introduced by the ATS pass'* on every swap and add, while Call 3 returns 0 characters for all five `final*` fields and Call 2 did the work; **(3) an unreconciled count** — stored `swap_decision` for opportunity `9f9c370a` reads kept 8, swapped 1, dropped 1, added 1, i.e. 2 of 10 finals differ from Call 1, while the recorded lineage shows Call 2 replaced **4**. Leading hypothesis, NOT established: `appPackets.ts:409` runs `applyCorrectionPass` before `writeSwaps`, so `buildSwaps` sees the CORRECTED package rather than the assembled one; `similarity()` at `SWAP_THRESHOLD` 0.5 also explains why one pair scores as `swapped` and the rest as drop+add, but not the missing rows. **That gap is a blocking precondition — building three-pass logic on an unexplained 2-vs-4 is the compare-two-proxies failure this repo forbids.** TIER 1: `swap_decision` carries `requirement_id`, `verbatim_quote`, `confidence` and `driver`, and `attribute()` scores overlap against the posting, so a row asserts a skill answers a specific line of the employer's text. `packetBuild.ts:85-93` already names this fix — *"attributing skills inside the swap system would be a genuine improvement to `skill_candidate.origin`"* — and says it was deliberately not done there. | The rows render and read as authoritative; the pass they credit is simply the wrong one, in a table whose whole purpose is saying who changed what and why. | Settle the 2-vs-4 count first, then extend `buildSwaps`. `check: manual db-query.yml — swap_decision counts for a rebuilt packet reconcile with packet.last_build.lineage` |
| D:packet-cannot-be-sent | OPEN | **CORRECTED 2026-08-22 — the row as first written was WRONG, and wrong in the way this repo has a standing rule against: it claimed a capability was ABSENT from a grep of ONE file.** `feedback` and `'applied'` are indeed absent from `appPackets.ts`, but SENDING IS BUILT AND WORKS — it lives in `appOutreach.ts`. `outreachSend` really sends through Microsoft Graph, `setOutreachState(messageId,'sent')` marks a copy-paste channel sent, and `Composer.jsx:85-99` is a real screen behind the "Send packet →" button. Better still, `appOutreach.ts:249-261` ALREADY GATES the send on packet QC: it joins `artifact`→`packet`, counts blocking findings and refuses with *"N packet asset(s) have blocking findings — resolve them before sending"*. So the QC layer already protects the outbound path, which is the opposite of what this row asserted. **WHAT IS ACTUALLY MISSING is a write-back and a note, not a half of the product:** (1) a send never marks the PACKET sent (`appPackets.ts:877` hardcodes `sent: false`), so `Packets.jsx:13`'s "Sent" group can never populate and a shipped packet still reads "Ready to ship"; (2) nothing sets `opportunity.stage = 'applied'` on send, which is why only **2 of 1,924** opportunities are `applied`; (3) "Request changes" (`PacketBuilder.jsx:194`) flips one artifact to `changes` with NO note, so a regenerate cannot know what to change. Review ROUNDS from the spec are genuinely absent, but they are an enhancement on a working path rather than a missing half. **MEASURED LIVE:** 39 packets — 0 marked sent, 0 with feedback; 195 artifacts — 0 approved. **(1) AND (3) BUILT 2026-08-22 (`aa4a42f`):** `markPacketSent` is the one writer of `packet.status='sent'`, wired at BOTH outreach write points (the Graph send and `outreachState`, which is the only route the copy-paste LinkedIn/call channels take); `recomputePacket` now treats `'sent'` as TERMINAL, without which it would have reset a sent packet to `ready` on the next artifact change; and the build response DERIVES `sent` instead of the literal `false` it returned for its whole life. "Request changes" now takes a note, appended to the long-declared-but-never-written `packet.feedback`, and `ensurePackage` feeds UNRESOLVED notes for that artifact type into generation — prepended as a directive exactly as `roleDirective` already is, so the Prompts table is NOT touched and `prompts['resume_user']` is still used verbatim. Notes resolve only after the package is stored. Guards `H:sent-is-terminal-and-written` and `H:changes-carries-a-reason`, seven mutations proven. **STILL OPEN:** (2) nothing sets `opportunity.stage='applied'` — deliberately NOT built, because outreach channels include `linkedinConnect`/`coldCall`/`followUp` and a connect request is not an application, so auto-advancing would mark the pipeline applied on a LinkedIn touch; and review ROUNDS from the spec. `check: manual owner — decide whether 'applied' is set by a deliberate action or by a specific channel` Found by reading `docs/design_handoff/proto-compass/packet.jsx` (the README names it the behavioural source of truth) against what is built — not by tripping over it, which is why no row covered it. The spec has review ROUNDS: request-changes bumps a round and prepends to a feedback thread, approve-all gates send, send moves the opportunity to `applied`, and a partner/reviewer seat leaves feedback during rounds. **Shipped:** `PacketBuilder.jsx:194` "Request changes" flips ONE artifact's status with no note and no round; `:784` "Send packet →" is `go('/compose/'+id)` — it NAVIGATES and writes nothing; `feedback` appears **0 times** in `appPackets.ts`; and `'applied'` appears in `api/src` only inside stage lists and a check constraint — **nothing in the packet flow ever sets it**. `packet.round` is written, but by `appRemediation.ts:488` counting remediation runs, not review rounds, and no `app/src` file renders it. **MEASURED LIVE:** 39 packets — **0 sent, 0 with feedback**; 195 artifacts — **0 approved**; and of 1,924 opportunities only **2** are `applied`. Thirty-nine packets built and not one has ever been approved or sent. | The build half works and looks finished — documents land in Drive, the QC rail renders, the button says "Send packet →" and navigates somewhere. The funnel simply ends there, and `Packets.jsx:13`'s "Sent" group can never populate. | `check: manual owner — decide whether review rounds and send are built now or the packet is declared build-only` |
| D:no-application-answer-assist | OPEN | **THE APPLICATION FORM ITSELF IS STILL ALL MANUAL, and it is where the built artifacts are supposed to land.** Raised by the owner 2026-08-22 while accepting the "Mark as applied" button: *"eventually we have to update the appl scraper answer generator extension which would support me in answering common/unique questions and attaching the items weve built."* The packet pipeline now ends at a declared application — build, QC, gate, send, mark applied — but the ACT of applying is the owner typing answers into someone else's form and hand-attaching files. Two capabilities are named and NEITHER exists in this repo: (1) **scrape the application's questions** from the posting/ATS form, and (2) **generate answers** to them, splitting COMMON questions (reusable across postings — work authorisation, notice period, salary, the "why this company" family) from UNIQUE ones that need the posting and this owner's profile; plus (3) **attach the artifacts already built** (`artifact.doc_url` for resume/compact_resume/cover/portfolio) rather than re-finding them in Drive. **What already exists to extend rather than duplicate:** `ownerFacts` and `requirement_evidence` are the answer substrate — a common answer is an owner fact, and a unique answer is the same evidence-resolution the QC layer already performs against a posting; `appJdParse` already parses postings; the artifacts already carry public Drive URLs. A parallel answer store would repeat the `taxonomy_title` mistake. **Deliberately NOT scoped yet** — an extension is a separate delivery surface (a browser extension, not this SPA) and the owner said *"eventually"*. | The pipeline looks finished because the packet is finished. The funnel's last mile is a human copy-pasting into a form, which no metric in this product measures. | Scope the extension as its own lane; first decide whether the answer generator lives in this API (so `ownerFacts`/`requirement_evidence` are reachable) with the extension as a thin client. `check: manual owner — decide the delivery surface and whether answers extend ownerFacts` |
| D:approval-needs-checks-that-never-run | CLOSED | **THE APPROVAL GATE DEADLOCKS THREE OF THE FOUR REQUIRED ARTIFACTS, so `ready` is STILL unreachable even after the video fix.** `approvalBlock` refuses approval with *"no checks have been run for this artifact"* — correct in principle, and it is the repo's own *absent evidence is `not_applicable`, never `pass`* rule. But **checks only ever run for `resume`.** MEASURED LIVE 2026-08-22, `check_result` joined to `artifact`: `resume` 60 check rows across **1** of 39 artifacts; `compact_resume` **0**; `cover` **0**; `portfolio` **0**; `video` **0**. Reproduced end to end: `POST /api/app/artifact/77d5e147.../status {"status":"approved"}` on the Trinnex cover returned **HTTP 409 `no checks have been run for this artifact`** (api-test run 32601711488) with the other three artifacts already approved. So `allApproved` cannot become true, `recomputePacket` cannot return `ready`, and `PacketBuilder` renders `Send packet →` only when ready — the SECOND structural block on the same path, found immediately after fixing the first (`H:readiness-ignores-unbuildable`). **THE FIX IS A REAL DECISION, NOT A PATCH, and it is tier 1 — it changes the approval gate.** Either (a) run a check suite for the other three types, or (b) `approvalBlock` demands checks only for types that HAVE a suite, treating a type with no suite as `not_applicable` rather than deadlocked. (b) is a gate weakening and needs owner sign-off; (a) is real work. Doing neither means no packet can ever ship. | The gate's refusal is a correct-sounding 409 that reads like the owner has one more step to do. There is no step: no path in the product runs checks for a cover or a portfolio. | **CLOSED 2026-08-22 — neither (a) nor (b): the engine already supported every type and nothing called it.** `evaluateArtifact` selects `a.type` and works from `pkg_json` + posting + profile, and appChecks own concurrency comments describe *"four artifacts of one packet"* entering it at once. So the gate was NOT weakened; `runPacketBuild` now runs checks per built artifact, after the evidence pass (the gate must read the rows that pass persists), sequentially (evaluateArtifact runs its own begin/commit on this client), each in its own try (four built documents must not be reported failed because one gate could not compute). Guard `H:build-runs-checks-so-approval-is-possible` FAILED on shipped code and passes now; removing the call fails it. `check: manual db-query.yml — check_result grouped by artifact type shows a non-zero count for every type approval requires` |
| D:mechanical-rules-have-no-enforcer | OPEN | **THE RUBRIC IS MEASURED BUT FOUR OF ITS SIX FAMILIES ARE ENFORCED BY NOTHING.** Raised by the owner 2026-08-22: *"why are these allowed to happen? don't the prompts need to be hardened or better systemized? otherwise I won't have one shot development and remediation loop to a ready packet."* Ground-truthed, and the answer is worse than a prompt-quality problem. The pipeline has exactly TWO automated correctors and **neither covers the findings that actually block the gate**: (1) `applyCorrectionPass` (`appCorrections.ts:92`) runs `scanEcho`/`planCorrections` — it fixes ONLY posting-echoed figures and claims the candidate does not own; (2) the remediation loop (`appRemediation.ts:185+`) is built entirely around COVERAGE — `coverageView`, `cov.openSeqs`, `scopeForRequirements(type, pkg, requirements, openSeqs)`, `CLOSE_CHECK_KEY` — it iterates uncovered REQUIREMENTS and knows nothing else. **So `skill_char_limit`, `relevant_char_limit`, `cross_list_redundancy` and `word_counts` have NO enforcement path anywhere in the system.** They are stated in the prompt, measured by the checks, and if the model does not comply there is literally no mechanism that makes it comply. **PROVEN LIVE:** remediation on the Trinnex cover (api-test run 32603441906) returned `closed: 0`, `editedFields: []`, `haltReason: no_coverage_evidence` — it halted having changed nothing, while 26 findings sat blocking the gate. **FIX DIRECTION (not prompt hardening):** these four are DETERMINISTIC and objectively decidable, so they belong in code, the way `applyCorrectionPass` already handles echoes — EXTEND that pass rather than adding a third corrector. `cross_list_redundancy` is a pure dedupe. `skill_char_limit`/`relevant_char_limit` need rewording, not truncation, so they need a tight single-purpose model call with a deterministic accept/reject on the length — never a general 'try again'. Asking one big prompt to satisfy a twenty-rule rubric in one shot is the design that produced this. | The prompt STATES every rule, so reading it suggests the rules are enforced. The checks MEASURE every rule, so the dashboard suggests they are governed. Nothing in between closes the loop, and the remediation loop halting cleanly with `closed: 0` reads like success. | Owner decision: extend `applyCorrectionPass` with a deterministic normaliser for the mechanical families. `check: absent api/src/functions/tests/appCorrections.ts cross_list_redundancy` |
| D:call3-compares-against-an-empty-list | CLOSED | **THE ATS PASS IS ASKED TO COMPARE TWO SKILL LISTS AND WE SUPPLY ONE.** Found by the owner's correction — *"the same prompt and models worked together fine. there is a nuanced difference in the pipelines"* — which is ground truth and reframed the whole investigation away from model quality. **Read from the live Prompts table** (`GET /api/prompts?key=ats_user`, api-test run 32605847835): `ats_user` v001, 8,807 chars, *"Seeded from Zap 289877647"*. Its stated objective is *"Identify and eliminate redundancy across Skills Lists A (1,2), Skills Lists B (1,2), and the Relevant Skills Lists"* and *"Compare each skill in Lists A to Lists B"*. The prompt interpolates **21 tokens; `pipeline.ts:401` `atsExtra` supplies 9.** Supplied: `ResumeSummary`, `skills list 1/2`, `Expertise`, `Relevant 1/2/3`, company, jobTitle — **all of them List A**. Blank: **`290709249__output__Item 13` and `__Item 15` (List B Skills 1 and 2)**, **`289877662__output__Item 41/43/45` (Relevant B 1/2/3)**, `289877662__output__Item 33` (JD responsibilities list), `289877662__output__Item 53` (extracted JD skills), the two memory-key tokens and the two cold-email contact answers. **So the entire comparison set is empty**, and the swap reasoning, the merged-list output and `changes_cited` all degenerate for that one reason. In the zap, node `290709249` produced List B; this pipeline has no equivalent node. **SECOND, SEPARATE MISMATCH in the same prompt:** it states *"Skills A and B items must be 24 characters or fewer"* and *"All Relevant Skills must be 20 characters or fewer"*, while `DEFAULT_THRESHOLDS.skillMaxChars` is **30** — our gate is LOOSER than the owner's own prompt, and 30 is the number the new normaliser was built against. | The prompt is present, the model is correct, the call returns, and the documents build — so every symptom points at model quality. Nothing surfaces that twelve of twenty-one tokens resolved to `''`. A model handed an empty List B still writes a confident merged list. | **RESOLVED FROM THE ZAP EXPORT + THE OWNER, 2026-08-22 — List B is NOT an external source and needs no new node.** Owner: *"list b is what we discussed previously. it is the result of what was kept from the original template items and any items that were swapped out. it's a post swap check."* The export confirms the mechanism: `290709249` splits `{{290709248__response__content}}` on `###`; `290709248` is *"Skills HTML Bullet List Formatting"* (`gpt-4o-mini`) whose prompt reads *"### Original Skills 1 ### - Re-format this bullet list ... {{289877662__output__Item 11}}"*; and `289877662` splits **`{{289877661__response__content}}{{299599701__response__content}}`** — Call 1 and Call 2 CONCATENATED. So List A (`289877667`, the trim loop) and List B come from the SAME generation, at different section indexes. **THIS IS `D33` SEEN FROM THE OTHER END.** `D33` records that `Skills1`, `Skills2` and `Relevant Skills 1/2/3` arrive as SECOND occurrences and are discarded, because `resumeParser.ts:155` is first-unfilled-wins and the owner's prompt asks the model to restate those lists inside the swap table *"before any swaps"* — the placed field is the post-swap final, the discarded copy is the pre-swap original. Those discarded second occurrences ARE List B. We already generate them on every build, drop them, then hand Call 3 an empty comparison set. The fix is to ROUTE what is already produced, not to rebuild a node. **CLOSED 2026-08-23 — List B is routed, and it needed no new node.** `listBFromCalls(c1, c2)` recovers the pre-swap copies from `_unmapped` using `headingKeysFor`, the SAME mapper `resumeParser` uses to decide which field a heading fills, and files them under the five tokens the prompt reads. **The bug the test caught before it shipped:** `LIST_B_TOKEN` was first keyed by merge-field names (`SkillsBullets1`) while `headingKeysFor` returns parser keys (`skills1`) — every lookup missed, `listB` was always `{}`, and List B would have shipped STILL EMPTY while the code read as wired. Guards: `api/test/listB.test.mjs` (7 cases, fixtures are the titles `D33` measured as discarded) and `H:list-b-reaches-the-prompt` for the caller. Not yet confirmed on a live build — the next `regen` says whether the ATS pass changes behaviour with a real comparison set. `check: grep api/src/functions/tests/pipeline.ts listBFromCalls\(c1, c2\)` |
| D:must-haves-are-guessed-when-the-posting-is-thin | OPEN | **THE MUST-HAVE SET IS A CATEGORY GUESS WHENEVER THE POSTING TEXT IS TOO THIN TO LOCATE REQUIREMENTS IN — and `must_have_coverage` then grades the packet against that guess.** Raised by the owner 2026-08-23 (*"the distribution of must haves seems more important"*), which was the right call: the coverage NUMBER was the symptom and the SET is the defect. **Mechanism, read from `requirements.ts:153` `mapKind`:** a row is `posting_required_marker` only when `REQUIRED_RE` matches its OWN text, and `category` when it matches the surrounding WINDOW. But `item_text` is a MODEL PARAPHRASE — paraphrasing strips "must have"/"required"/"N+ years" — so the own-text test rarely fires, and the window test needs `char_start`, which only exists when the row was LOCATED in the posting. An `unlocatable` row therefore has no window, and `mapKind` falls through to `must_have` / `category_default` every time. **MEASURED on Trinnex `9f9c370a`:** `jd_real` is **NULL**, `raw_jd` is **1,054 chars**, **4 of 5 must-haves are `unlocatable`** with no verbatim and no offsets, and **all 5 are `category_default`** — zero from any posting marker. So "1/4 must-haves evidenced" grades against five requirements the posting never asserted. **SYSTEMIC SCALE — CORRECTED 2026-08-23, the original figures here were WRONG:** the "1,941 opportunities / 31% lack `jd_real`" count was taken over ALL rows including dismissed, demo and other owners. Scoped to the owner's ACTIVE pipeline (`not dismissed and not is_demo`), run 32614116061: **1,114 total, 1,030 (92.5%) have `jd_real`**, only 84 do not — 83 of which have NO `job_id` at all (Indeed 34, LinkedIn 27, Email 21, Extension 1) and so have nothing the guest endpoint can fetch. The backfill queue is **EMPTY (0 pending)** and the fetch log shows 1,526 `ok_jd`. **The backfill is not broken and `jd_real` is not the bottleneck.** Of 9,196 requirements; 492 (25%) have `raw_jd` under 1,500 chars; `unlocatable` is **1,474 (15%)**; `category_default` is **2,196 (22%)** against `posting_required_marker` at just **1,080 (11%)**. So the machinery is sounder than Trinnex suggests and Trinnex sits in the worst quartile — but a fifth of all requirements are classified by category default rather than by anything the employer wrote. | Every layer downstream reports confidently. The requirements table is populated, the checks run, the gate fails with a specific number, and nothing says the must-have SET was never grounded in the posting. `kind_source` records it faithfully and no screen reads it. | Fix is UPSTREAM of the checks: get real posting text (why is `jd_real` NULL for 31%?) before tuning coverage. Surfacing `kind_source` on the QC rail would also stop a guessed must-have reading like an employer requirement. `check: manual db-query.yml — kind_source distribution; category_default share must fall as jd_real coverage rises` |
| D:every-build-is-destructive | OPEN | **NO VERSION OF AN ARTIFACT SURVIVES A REBUILD, and a column implies otherwise.** `set content = $1` at `appPackets.ts:217`, `:1064`, `:1131`; `pkg_json` overwritten at `:419`, `:1071` and `appRemediation.ts:266`. `artifact.version_history` is appended to at `:218` with **`{"len": N}` only** — a length, not the text — and nothing reads it. The spec lists version history as a first-class artifact field and the prototype shows a restore link (`packet.jsx:337-344`, itself a mock with no `onClick`). The one real exception is `insertion`, keyed `unique (artifact_id, merge_field, loop)`, which does keep before/after per remediation loop — and `D:remediation-never-ran` records that table as empty in production. | A `version_history` column exists and is written on every build, so a reader concludes the prior text is recoverable. It is not: the column holds a character count. | `check: grep api/src/functions/tests/appPackets.ts version_history = coalesce.*'len'` — the row closes when the write stores the prior content instead of its length. |
| D:no-template-picker | OPEN | **NARROWED 2026-08-24 — the dead-column half is FIXED; the picker half is not.** The row made two claims. (1) `artifact.template_id` was dead: SELECTed at `appPackets.ts:77`, projected as `templateId` at `:125`, and **written by nothing** — measured live, 195 artifacts, 0 with a `template_id`. That is now false. `renderArtifact` writes it from `meta.templateId`, the OWNER-RESOLVED id (`google.resumeTemplateId` or the per-role override), so the row records the document the packet was actually built from rather than the one a default says it should have been. (2) **STILL TRUE: there is no per-artifact template picker.** `packetTemplates.ts:22-39` still hardcodes one id per TYPE, overridable only globally per owner (`metaFor:67-72`), and no route accepts a template id for a single artifact. `D32` ruled the resume TEMPLATE decides the role focus every prompt is prefixed with, so which template an artifact uses is a content-correctness question, not a convenience — that is why the remaining half stays open. | The column is now written and returned honestly, so the API no longer lies about it. But a reader still cannot CHOOSE a template per artifact; the value they see is the one global setting resolved at build time. | `check: absent api/src/functions/tests/appPackets.ts body\?\.templateId` — the row closes when a route accepts a caller-supplied template id for one artifact, which is what a picker requires. |
| D35 | CLOSED | **CONFIRMED LIVE, END TO END.** The build no longer runs inside the request. It reproduced twice — run **32546312184** returned 504 at 02:31:51 with every artifact already written (02:29:02 / 02:30:10 / 02:30:53 / 02:31:50, all four with doc_urls), one second after the last one landed; run 32548283352 returned 502. The work completed and only the answer died, which is why nothing looked broken for a day while the owner was shown a failure and paid to build it again. Fixed 2026-08-22 (`6050fff`, `96e2f06`, on `main` at `ca83b00`): `POST packet/build-async` files a job and returns 202; `buildQueueTick` claims one job a minute and runs the SAME `runPacketBuild` the synchronous route runs — extracted, not copied, so the two cannot drift. The sync route is untouched because `appBulk` and the coach tool call it. The UI queues and polls, and reports the three enqueue outcomes distinctly. **Six defects in the queue were found by an independent AC pass and are now tests** (`api/test/buildQueueDb.test.mjs`, 11 against real PostgreSQL, five mutations proven to bite): the attempt cap sat outside the claim's subquery, so one poisoned job returned "queue empty" and stopped every build for every owner; `finishBuild` discarded the payload on failure — throwing away the partial-build evidence this queue exists to preserve; it was unfenced, so a reclaimed zombie could overwrite a live run; a rebuild behind a live cached build was silently downgraded to it; `enqueueBuild` could return an absent job while its type promised one; and a job could be filed against another owner's opportunity. **MEASURED IN PRODUCTION, 2026-08-22.** Job `945e28ed` (regen): `HTTP 202` sub-second, created 03:57:41.97, **claimed 03:58:00.08 — 18s, by the queue message, not a tick**; the first worker then died silently at the Consumption plan's 5-minute default `functionTimeout` (staged fix `d1a16b3` raises it to the plan maximum of 10 minutes), and **the sweep reclaimed the wedged job at exactly 04:10:00.14 with `attempts → 2`** — `H:dead-worker-does-not-wedge-a-build` proven in production, not just in a test. Attempt 2 finished in 196s with four artifacts, 42 warnings and `evidence {total 10, evidenced 6, proposed 5, escalated 8, refused 0}` — **and the payload survived on a non-success job, which is the whole point of the row.** Job `fca4de3c` (regen:false, deploy `b258a4e`): `wake_secs 0`, **`state: done`** in 36s, four artifacts, evidence resolved. **One defect was found by running it and is fixed in `b258a4e`:** the worker read `summariseBuild().ok` as success, but that flag means CLEAN — no warnings at all — so the 42-warning build above was recorded `failed` and the owner would have been told their build failed while looking at four finished documents. `buildJobOutcome` now fails only on an artifact that did not build, nothing built, an error, or a non-200; `H:warnings-are-not-a-failed-build` pins the measured payload. | The owner's Build/Rebuild returns immediately and reports what actually happened, including on a partial failure. The worker is woken by an Azure Storage Queue message (`packet-build-jobs`) sent as the job row is written — the owner asked for the storage account rather than the one-minute tick the first version used, which made every build wait up to 60s for an event whose exact moment we already knew. The timer survives as a five-minute SWEEP, scoped to the one case no message can announce: a worker that died mid-build. | `check: manual api-test.yml — POST /packet/build-async, then GET /packet/build-job/{id} reaches state=done with four artifacts; no sandbox can reach the Function or the queue` |
| D:chk-settings-have-no-writer | CLOSED | ~~Four evidence knobs are per-owner columns that NOTHING in the product can write~~ **CLOSED — all fourteen are writable and have controls.** The fix was one writer for the whole `chk_*` family rather than a fifteenth exception, EXTENDING the route that already owns `owner_search_prefs` (`appSearchPrefs`) rather than standing a parallel one beside it. The whitelist is DERIVED from the statement that declares the columns (`checkPrefColumns`), so a knob added later is writable and rendered the day it is added — which is the property that stops this gap regrowing one setting at a time, the way it grew. Column names are interpolated (SQL cannot parameterise them) but always the whitelist entry that MATCHED, never the caller's key: `H:settings-writer-takes-names-from-the-whitelist-not-the-request` drives `updated_at=now(), owner_email`, a trailing `; drop table requirement--`, `target_geo_ids`, `temp_hot_hours` and `__proto__` and asserts none reaches the SQL. Values are coerced by the column's declared type; NaN is dropped so the previous value survives. **H42's KNOWN list of unwritable settings is now EMPTY** — and H42 itself had to be fixed in the same change, because its scan reads literal `col =` text and a whitelist-driven writer has no literal name to find: it would have gone on reporting fourteen columns as unwritable while a route wrote them. It now establishes reachability, with a floor so a broken derivation fails as vacuous. **VERIFIED LIVE** (deploy `c230f30`, ui-verify **32542401407**): the escalation toggle and the citation-width control render for the real owner. | — | — |
| D:openai-transport-duplicated | OPEN | **Thirty-one modules each build their own OpenAI HTTP call.** `grep -rl api.openai.com api/src/functions` returns **31 files** (`appReviewer`, `appPackets`, `appJdParse`, `coachAgent`, `mailWatch`, `appVoice`, the whole `mt*` harness, ...), each carrying its own copy of the same twelve lines: key from env, JSON response format, non-2xx handling, and — where it happens at all — the `logUsage` call. They do NOT agree with each other: some meter and some do not, model and temperature defaults differ, and the non-2xx branch differs between copies, so an outage, a price change or a metering fix has 31 places to reach. `openaiJson.ts` (2026-08-21) is the ONE implementation, written for the escalation tier rather than as a refactor — a 32nd copy was the alternative, and this lane does not own the other 31 working call sites. | Every one of the 31 works, so nothing is failing and the duplication is invisible from the outside. A new module reaching for the nearest existing copy reads as following the established pattern, which is exactly how the count reached 31. | Each lane that touches one of those files moves it onto `openAiJson` in the same commit. The count is the progress measure and may only fall. `check: manual lane — the count from grep -rl api.openai.com api/src/functions must not exceed 31 and falls as lanes migrate` |
| D:proposal-reasoning-unverified | CLOSED | ~~The escalation tier stores a model-written justification that nothing checks~~ **CLOSED — split by what can be settled exactly.** The owner chose (a), verify-and-drop; built and MEASURED against the real cases first, (a) as specified failed in both directions — it missed the security case entirely (`sameWord('secure','security')` is false) while dropping that row on the unrelated token `software`, and it fired three times on sound reasoning. An independent review found the sharper consequence: a test asserting *case 1 is dropped* would have gone GREEN while the guard was blind to the defect that commissioned it. **SHIPPED SHAPE:** ACCUSE only on `namedEntityTokens` — an acronym or product name (`IoT`, `AI/ML`, `SOC 2`) is present in the excerpt or it is not, with no morphology to get wrong, which is why `supportIn` already treats named entities as absolute; a sentence crediting the excerpt with one it does not contain is WITHDRAWN and replaced by the fact that withdrew it. LABEL everything else with the deterministic note `resolveEvidence` already publishes, so the owner reads the model's claim beside a computed statement of what the excerpt does not mention — which reaches the security case that no exact rule can. `extra` is never blanked: `verifyProposal` refuses an unexplained match (`no_reasoning`), so a null note would contradict this module one function up. Guards `H:named-overclaim-is-withdrawn`, `H:no-vacuous-reasoning-gate` (asserts the security case is NOT reported as caught — the assertion that cannot pass by accident) and `H:reasoning-check-never-cries-wolf` (sound reasoning survives, INCLUDING an explanation that honestly says the excerpt does not address security). Reverting to the naive all-token version fails three of them. **NOT VERIFIED LIVE** — the next escalation run confirms it. | — | — |
| D:reasoning-semantic-judge | OPEN | **The switch to pull if the named-entity rule proves too blunt.** `verifyReasoning` accuses only where a string is present or absent, so it reaches `IoT` and `AI/ML` and declines everything else — including the `secure`/`security` case, which it labels rather than judges. That is the honest limit of token matching, not a gap to paper over, and the owner accepted it: *"for now 1 and document 2 as a potential switch if it is problematic."* **WHAT WOULD MAKE IT PROBLEMATIC**, and it is worth naming so the trigger is observable rather than a feeling: (i) `reasoning_withdrawn` stays at zero across many runs while labelled rows keep containing fabricated clauses — the exact rule is too narrow to be worth its complexity; or (ii) the owner finds themselves reading the label and mentally discounting every proposed row, which means the note is doing the job of a refusal without the honesty of one. **THE SWITCH:** a second model call that judges ONLY whether the explanation's claims are supported by the quote — a narrow, checkable question, unlike asking a model to judge relevance. **The objection to it is real and is why it is not built:** a model policing a model's prose relocates the problem rather than solving it, and this repo's rule reserves accusation for exact rules. It would need its own falsification harness — the judge's verdicts checked against a hand-labelled set — before any verdict of its could withdraw text. | The tier reports `reasoning_withdrawn` counts, so it LOOKS like the explanations are being policed. They are policed only where an acronym settles it; everywhere else the owner is being handed a fact and asked to judge. | Owner, when a run makes (i) or (ii) concrete. `check: manual owner — pull this switch only on observed evidence that the named-entity rule is too narrow, never on principle` |
| D:deterministic-evidence-reaches-nothing | OPEN | **THE DETERMINISTIC RESOLVER EVIDENCES 0 OF 35 REQUIREMENTS against a healthy profile — this is what actually holds `must_have_coverage` at zero.** Measured 2026-08-23 on eMoney `2cb56fb3` (real `jd_real`, 9,749 chars, 12 coverable must-haves). The profile reads fine — `sources: ["resume template 1bwOcxvk…", "MasterContext (14 blocks)"]`, `profileReadable: true` — and the requirements are good: 8 of 13 must-haves carry a real posting marker (`posting_required_marker` / `category`), all from `jd_real`. Yet `writeEvidence` returned `evidenced: 8` where that figure is `deterministic + proposed` and `proposed` was **8**, so **deterministic evidence was 0**. All 12 open must-haves hit the escalation cap; the model proposed 8, declined 4, and 4 more were withdrawn for reasoning. Because `ruleEvidenceOf` excludes `proposed` BY DESIGN ("a model may PROPOSE, only an exact rule may ACCUSE"), coverage reads **0/12**, and `responsibilities_addressed` **0/21**. **Not the exemplar and not the wipe:** Trinnex (a 1,054-char extension snippet) scored 1/4, so the REAL posting scored WORSE — the thin-JD explanation is falsified. The wipe (`D:evidence-is-deleted-by-its-own-build`, FIXED) explains why nothing accumulated; this explains why there was nothing to restore. **Coverage cannot rise until this is diagnosed.** Suspect the reach of `requirementSupport` / `EVIDENCE_THRESHOLD` against real posting verbatims — a requirement like "Experience driving DevOps, SRE, and platform engineering transformations" (7 content tokens) versus profile prose that paraphrases it — but that is UNVERIFIED and must not be tuned before it is measured. | Every packet shows the owner 0 must-haves evidenced and 0 responsibilities addressed no matter how good the resume is, so the gate can never go green on merit and the QC rail teaches the owner to ignore it. | Measure BEFORE tuning: take the 12 eMoney must-haves and the 15 profile records and compute the actual token-overlap ratio for each pair. If the best ratio is just under the threshold, that is a calibration fix; if it is near zero, the matcher is looking at the wrong text. `check: manual api-test.yml` |
| D:proposals-can-never-be-confirmed | CLOSED | **The product tells the owner a model proposal is "awaiting your confirmation" in THREE places, and there is nothing anywhere to confirm it with.** Swept all three layers 2026-08-23: **no route** (`grep app.http` across all of `api/src` for confirm/accept/approve/vouch → nothing), **no column** (`requirement_evidence` has no `confirmed_at`/`accepted`; `owner_fact` DOES have `confirmed_at`, so the pattern exists and was simply never applied here), **no UI** (nothing in `app/src` pairs confirm with proposal). The strings that promise it: `appRequirements.ts:774` "…awaiting your confirmation; they are shown but do not count toward the coverage gate", `checks.ts:671` "N model-proposed, awaiting your confirmation", `checks.ts:684` "a model proposes \"…\"; confirm it". **Why this is THE blocker and not the matcher:** measured offline against the real profile excerpts (`api/test/tools/measure-matcher.mjs`, reproduces all 12 live refusals exactly), the best excerpt for each of the 12 eMoney must-haves scores **0.00–0.50 against a 0.70 bar**, and the misses are mostly words the profile genuinely does not contain (`executive`, `capable`, `modern`, `patterns`, `devops`, `sre`, `exceptional`, `communication`). Derivational stemming — the fix I was about to build — moves seq 29 to 0.60 and seq 33 to 0.67 and **clears ZERO of the twelve**. Lexical matching cannot bridge posting vocabulary to profile vocabulary; that is a property of the two texts, not a tuning error. Meanwhile the escalation tier ALREADY solves it semantically — it proposed evidence for **8 of the 12**. So the design intent (model PROPOSES, human ACCUSES) is correct and complete except for its last step, which does not exist. | Coverage is structurally pinned at 0 forever. Every packet shows the owner "0/12 must-haves evidenced" no matter how good the resume is, the gate can never go green on merit, and the QC rail teaches the owner to ignore it. The owner has been blocked for two days by a promise the UI makes and the schema cannot keep. | Build the confirmation path — it preserves the house rule exactly, with the HUMAN as the accuser rather than the model: add `confirmed_at`/`confirmed_by` to `requirement_evidence`, a confirm/reject route, the QC-rail control, and let `ruleEvidenceOf` count a CONFIRMED proposal (never an unconfirmed one). TIER 1. **CLOSED 2026-08-23, verified live.** `evidence_confirmation` keyed on CLAIM IDENTITY (requirement text + source_key + offsets + quote + record_sha256), so it survives the `delete from requirement` cascade but lapses the moment the profile or the requirement changes; `ruleEvidenceOf` counts a CONFIRMED proposal and still never an unconfirmed one. A SECOND defect had to be fixed for it to matter at all: the escalation cap was spent in `seq` order, so all 8 proposals landed on responsibilities (seq 0-11) and ZERO on must-haves (seq 22-34). Measured on 2cb56fb3: proposals on must_have went 0 to 5, and confirming two moved `must_have_coverage` from "0/12 must-haves evidenced" to "2/12". `check: grep api/src/functions/tests/checks.ts isConfirmed` |

## Owner-deferred and owner-requested, 2026-08-23/24

**Why this section exists.** These were recorded as PROSE in `.claude/actions.md` as they arose and
never entered this ledger, so nothing re-checked them and no single list existed. The owner asked
for one and it could not be produced: *"what else did I tell you to save until later? you should
track them all... it seems you don't know what the comprehensive list is despite you saying you
recorded it all up front."* That is precisely the failure rule 2 at the top of this file names —
adding a row is part of the commit that defers the work. Two of these are worse than deferrals: the
owner ASKED for them and they were not built.

| # | Status | Not done | What makes it look done | Trigger / check |
|---|---|---|---|---|
| D:resume-summary-band | OPEN | **OWNER REQUESTED, NOT BUILT.** *"go ahead and add the 55-60 setting. use the current character count in the template as a guide if ever unsure."* `WORD_RULES` (`checks.ts:478-484`) carries five fields — `@AboutMe1_50words`, `@AboutMe2_60words`, `@ExecutiveProfile_55words`, `@CoreAccomplishments_5blts_180words`, `@CoverLetterBody` — and `ResumeSummary` is not one, so no band is enforced and none is shown beside the field. Measured in production: the seven ResumeSummary values are 48, 49, 49, 61, 61, 70, 70 words — **none inside 55-60**, so this is not academic. Must be an owner-settable `chk_*` pref like every other threshold ("all such rule numbers need to be available for tweaking in the settings/config"), never a literal. | Every OTHER field states and enforces a word band, and the resume step now shows a target beside each measurement, so the one field without one reads as deliberate. | **BUILT 2026-08-24, NOT VERIFIED LIVE — which is why this row is still OPEN.** `resumeSummaryWords` seeded [55, 60] from Prompt 16 itself, owner-settable as `chk_resume_summary_words_min/max`, enforced by `word_counts` and stated beside the field by `targetFor`. The ledger guard caught this row going stale the moment the code landed — its `absent` check flipped false in the same commit that built the thing, which is exactly the staleness this file exists to refuse. It now waits on a live render. `check: manual ui-verify.yml — the band renders beside ResumeSummary on the resume step` |
| D:gate-advisory-not-confirmed-off | OPEN | **OWNER REQUESTED, NOT CONFIRMED DONE.** *"turn that advisory mode off, I am looking for the experience in the design spec."* `DEFAULT_THRESHOLDS.gateAdvisory` is `false` (`checks.ts:152`) but the stored value is per-owner (`chk_gate_advisory`, `checkPrefs.ts:62`) and von.ellis's row measured **true**. The current live value has NOT been re-read — the brokered DB connector lapsed mid-session — so this is unconfirmed in both directions, which is not the same as done. | The code default reads `false`, so a grep of the source says advisory mode is off. The DEFAULT is not the owner's ROW. | Read it, then flip it if still true. `check: manual db-query.yml — select chk_gate_advisory from owner_search_prefs where owner_email='von.ellis@enterpriseds.io'` |
| D:jd-digest-source-unknown | OPEN | **Something outside this repo fills `raw_jd` with a ~1KB DIGEST, and it has not been identified.** Trinnex `9f9c370a`: `jd_real` 0, `raw_jd` 1,054 against a real 8,619-char posting, so every requirement was extracted from a summary — 5 of 10 unlocatable, both real Minimum Qualifications absent. ELIMINATED by exhaustive grep: `jdSearch`, `appAts`, `appCapture`, `fetchPageText` (plain fetch + `normalizePostingText`, no LLM), `jdBackfill`, the Zapier export (`docs/zap-289877647/`, zero hits for the template strings, `raw_jd` or `jd_real`), and every repo-wide match for the digest's own headings. **The owner ruled out n8n explicitly** — *"it's not n8n your wrong"* — and deferred the hunt: *"we'll deal with this once we have the UI matching the he prototype."* | `jd-parse` refreshes `jd_text` correctly from whatever `resolvePostingSource` returns, so the pipeline looks healthy; the damage is upstream of it. Fleet-wide, 1,531 opportunities carry `raw_jd` and 215 carry neither source. | Owner, once the UI matches the prototype. `check: manual owner — the writer is outside this repo and the owner deferred the hunt` |
| D:jd-field-renames | OPEN | **`jd_text` should be `jd_summary` and `jd_real` should be `jd_raw`, and it is unsettled whether `jd_real` and `raw_jd` need to both exist.** Owner: *"jd_text should be jd_summary. je_real should be jd_rqw and I'm not sure why we need both that and raw_jd if they are exactly the same."* The names actively mislead — `jd_text` is a DERIVED cache and reads as the posting; a session already wrote a real posting into it believing it was the source of record. Rename surface measured: `jd_text` 16 sites in `api/src`, `jd_real` 56, `raw_jd` 30, across 32 files. Deferred with the digest hunt in the same message. | `resolvePostingSource` (`jdText.ts:85`) already implements the correct precedence `jd_real` → `raw_jd`, so behaviour is right and only the NAMES lie. | Same trigger as the digest hunt, and best done with it since both turn on which column is the source of record. `check: grep api/src/functions/tests/jdText.ts jd_text` |
| D:req-seq-one-based | OPEN | **The owner wants human-friendly 1-based requirement numbering and the change is TIER 1.** *"I do want human-friendly 1-based numbering"*, then, on hearing the scope: *"I didn't realize it would take that much. we can track this to knock out when the original resume gaps are closed."* Display is now internally consistent at 0-based (PR #48, one shared `reqChipLabel`). Going 1-based means changing SEVEN api writers (`checks.ts:588/594/616/680`, `dimensions.ts:286`, `reviewer.ts:504`, `remediation.ts:539`) AND `offenderSeq()`'s parse together — **and `check_result.offenders` is PERSISTED** (`schema.ts:578`), so rows already in production carry 0-based `#N` and would be misparsed by one, silently attaching a finding to the wrong requirement on the path that feeds `openSeqs` and the coverage cards. | The chips are self-consistent now, so nothing looks broken and the change reads as cosmetic. It is not: the same number is a display label AND a join key. | After the resume gaps close. Needs the AC pass that was started and stopped, and a decision between storing 1-based, displaying 1-based, or a format-versioned migration. `check: manual owner — the owner deferred it until the resume gaps close` |
