# Deferred work — the ledger of what is NOT done

**Why this file exists.** Twice in one session, work that was never done read as done. The P3
remediation lane died without pushing and the tracker went on listing it as in flight. An
`actions.md` entry said the "Re-run ATS analysis" button could not re-run months after it was fixed.
Both failures are the same shape: **a claim about state that nothing re-checks.**

Every row below is something a reader could reasonably assume is finished and is not. Each names
**what makes it look done** — because that is the thing that will mislead the next session — and the
**trigger** that should make someone pick it up.

Rules:
1. A row leaves this file only when the work is done AND verified, or when the owner decides it will
   never be done — in which case it moves to a "won't do" line with the reason, never just deleted.
2. Adding a row is part of the commit that defers the work. Deferring silently is the failure.
3. `git branch -r` is the proof a lane exists. A tracker entry is not.

---

## P8.2 / R3 — figure echo

| # | Not done | What makes it look done | Trigger |
|---|---|---|---|
| D1 | **The rewrite half of R3 does not exist.** The backlog asks that an echoed figure be REPLACED (`60+` → the candidate's `62`) or generalized (`$18M` → `8-figure`), each logged and revertible. Only the *scan* is built. | The check runs, names offenders, and reads as complete on the QC rail. The PR says "deferred to P8.1", which is half true — the LOGGING needs P8.1's correction table, but the substitution logic is absent independently of it. | P8.1 lands |
| D2 | **`generalize()` has zero production callers.** Tested dead code. | It exists, it is exported, its tests pass, and its outputs match the AC examples exactly. | P8.1 lands |
| D3 | **Nothing computes the candidate's substitute figure.** `scanEcho` keys the profile side on the *exact* figure, so it can answer "does the profile also say 60?" but never "what is the candidate's corresponding number?" This is structural, not a missing function. | `shared[].profileRaw` looks like the substitute. It is the opposite: the figure that must NOT be replaced. | Before D1 — needs a design decision, and a resolver that guesses is worse than generalizing |
| D4 | **Non-numeric echoes are not detected.** The spec asks that wording lifted from the ad be listed separately as "wording kept from the posting", as a user judgement call. | R3 reads as "the echo check" and is green. | P8 close-out |
| D5 | **`swap_decision.to_label` is never figure-scanned.** `runChecks` scans rendered `pkg` fields only, so a swap recorded but not yet reflected in a field is unchecked. | C3 is satisfied for rendered list items, which is the case everyone tests. | When swaps and rendering can diverge in time |
| D6 | **Extraction gaps a verifier measured:** `thirteen, fourteen, sixteen, seventeen, eighteen, nineteen` are absent from `SPELLED`; `USD 18M`/`EUR` are not currency (`[$£€]` only); `18M` without a symbol never collides with `$18M`. | 300 tests pass and every AC-named form is caught. | Any real-corpus scoring run |

## X2 / regen reachability (`claude/qc-regen-reachability`)

| # | Not done | What makes it look done | Trigger |
|---|---|---|---|
| D7 | ~~No independent verifier~~ **DONE.** It found six defects in the guard itself, all fixed: a live false negative (`apply`), a surviving global-name fallback, six missed grammars, a cry-wolf on query-string routes, a staleness floor that could not detect staleness, and the Rebuild affordance having zero test coverage. | — | — |
| D20 | **A bare truthy body read (`if (body?.x)`) is out of H33's scope.** `appFacts.ts:232` reads `body.confirm` that way and is invisible to the guard. | H33 is green and its band is tight, so it reads as complete coverage. | Any future toggle written in the truthy form. Deliberate: including it accused three string-presence checks (`alertText`, `imageB64`, `demoState`) of being unreachable toggles |
| D8 | ~~Structural guard not written~~ **DONE** — H34 (`a38c94f`). Took four wrong versions: vacuous, cried wolf, missed a dispatcher, too permissive again. Found a real fourth instance (`ats_source.enabled`), now wired. | — | — |
| D9 | ~~No live verification~~ **DONE, proven live.** Identical requests differing only by `regen`: `{}` → **0** model calls (cache served); `{"regen":true}` → **3** (`:resume` 9,585 tok, `:portfolio` 11,522, `:ats-qc` 6,562). Precondition checked first (`jd_grounded=t`, `has_pkg=t`) so `staleUngrounded` could not mask it. **The recommended evidence was wrong**: `packet.updated_at` moved on the CONTROL too (00:44:30 → 16:41:35) while the content stayed byte-identical, so it is not a regeneration signal — only `usage_metering` is, because a cache hit cannot make a model call. | — | — |

## Cross-lane

| # | Not done | What makes it look done | Trigger |
|---|---|---|---|
| D10 | **H-case IDs. PRE-ALLOCATION HAS NOW FAILED THREE TIMES** — one-per-lane (too few), then a reserved RANGE for a branch that had not landed, which `H26`'s contiguity rule correctly rejected as a gap and turned PR #14 red. My instruction, not the lane's error. **The rule is now: numbers are claimed AT MERGE TIME, never reserved. Merge `origin/main`, run `H26`, take the next contiguous numbers.** Earlier state: `H26` now asserts one-ID-one-case AND contiguity, and it fired on its first run. P8.2 renumbered to land contiguous at H1..H27. **The other two lanes still collide and must renumber before merging: P8.3 starts at H28, P3 after it.** | Every branch is green in isolation, and `hardening.test.mjs` is append-only by convention, so the merges apply CLEANLY and duplicate silently. | Each remaining merge, in merge order |
| D11 | ~~P7 items 4, 6, 8~~ **ITEM 8 DONE; ITEM 4 GROUND-TRUTHED, REMEDY DEFERRED TO THE OWNER (see D29); item 6 was already done.** Item 8: `CONFIG_KEYS` extended with the six RowKeys the Auth & Config screen has been writing all along (`google.resumeTemplateId` / `portfolioTemplateId` / `coverLetterTemplateId` / `outputFolderId`, `microsoft.senderEmail` / `recipientEmail`) — they were written by the console and read by NOTHING, which is worse than not existing because it tells the owner they are in control when they are not. `pipeline.ts`'s byte-identical second copy of the four Drive id literals is gone; `packetTemplates.ts` is the one home for the seeds. `renderArtifact` (the path that actually ships) resolves its ids the same way. Guarded by `H:owner-config-is-read` (behavioural: six DISTINCT owner values through `settingsFromConfig`) and `H:no-second-id-copy`. **NOT verified live** — the sandbox cannot reach the Function App. | — | — |
| D12 | ~~200 with `pass:false`~~ **DONE, split deliberately by kind — and one half of the decision is DISPUTED, see D30.** The CATCH exit (an exception aborted the run: no urls, job row still `processing`) now returns **502** — that was simply wrong as a 200. The COMPLETED-but-not-clean exit keeps a 2xx and the CALLER is fixed instead: `api-test.yml` exits 1 when the body reports `pass:false`. That is the general half — 85 routes here return a `pass` boolean and the workflow ignored every one, so a status change on one route would have closed one case in eighty-five. `pass` only, never `ok`: `ok:false` is a REFUSAL in `appRemediation` and firing on it would be the cry-wolf failure. Guards: `H:run-outcome-distinguishable` (pure `runOutcome`), `H:pipeline-error-status` (invokes the REAL handler in-process), `H:pass-false-is-red` (EXTRACTS `body_failed` from the YAML and EXECUTES it under python3 against 8 bodies). **PROVEN LIVE, as an A/B against the same Function App** — the live API still runs main's code, so the identical request `POST /api/pipeline/run {"jobId":"no-such-job"}` (which throws on the job lookup, before any Drive or mail call) shows the defect and the fix side by side: ref `main` = run **32437830271** `success`; ref `claude/qc-p7-hygiene` = run **32437836338** `failure`, logging `HTTP 200 ... "pass": false` and then `FAILED: HTTP 200 but the response body reports pass=false`. Control against cry-wolf: `GET /api/health` at the same ref = run **32437724855** `success`. The ROUTE half (502) is NOT verified live — it is not deployed. | — | — |
| D13 | ~~`Promise.all(docJobs)` orphans Drive files~~ **DONE for the failed-build case; the per-rebuild superseded file is UNCHANGED and still leaks — see D31.** `deleteDriveFile` is now the only Drive DELETE in `api/src` (there were zero). `copyThen` owns copy-then-work so a post-copy failure can delete the file whose id previously existed only in the frame that threw — the half a naive `allSettled` still loses. `buildAllOrCleanUp` uses `allSettled` because `Promise.all` rejects on the FIRST failure while siblings run on, which is what made the ids unenumerable at all. Applied to BOTH `pipeline.copyAndInject` and `appPackets.renderArtifact`. Guards `H:orphan-drive-files` (a job that fails FAST while siblings resolve LATE) and `H:orphan-after-copy` (stubbed fetch, asserts the DELETE was issued and the original error survived). **NOT verified live** — no Drive call has been made from this branch. | — | — |
| D14 | **`covered_kw` does not mean covered.** The prompt asks for "ATS keywords for this role" with no candidate comparison, and the array renders as green "N covered" chips. | It renders as a coverage number, in green. | P8 close-out |
| D15 | ~~Stale label~~ **DONE.** `assetGate.js` now reads "Must-haves your profile can evidence" and the score card "Must-haves evidenced". A label describing the previous definition next to a correct number is the half a reader believes. | — | — |
| D16 | **PARTIALLY FIXED on `claude/qc-live-defects`; the honest fix is still blocked.** `appReviewer` no longer maps every `kind === 'must_have'` row: `judgedMustHaveIds` (artifactScore.ts) reads what the check PUBLISHED about the population it judged — the `<covered>/<judged>` denominator in `must_have_source`, and `uncovered_requirement_ids` — and returns a set that is never a superset of the truth, so a row the engine did not judge is `not_comparable` rather than agreement. It does NOT recompute `coverable`; that predicate is checks.ts's and a second copy is the R4 defect. Two branches, both sound: `judged === the must-have count` gives the exact set; otherwise only the uncovered ids are known to have been judged and the judged-COVERED rows stay incomparable — understating agreement rather than inventing it. **What is still not done:** `artifact_score` has no column recording which rows were judged, so that conservative branch loses genuinely comparable rows. The complete fix is three lines and needs two files this lane was told not to touch: `judged_requirement_ids uuid[]` on `artifact_score` (**schema.ts**), filled beside `uncoveredIds` in `evaluateArtifact` (**appChecks.ts**). `judgedMustHaveIds` already PREFERS `judged_requirement_ids` when the row carries it, so landing those two lines completes it with no further change here. `H44` guards the helper, `H44b` the writer/reader round trip, and `api/test/appReviewer.test.mjs` guards the CALL SITE end-to-end (H44 alone does not catch reverting the caller — proved by reverting). | The conservative branch is invisible: agreement simply reads lower, with an honest "N not comparable" beside it. | The `schema.ts` / `appChecks.ts` lane lands |
| D29 | **`portfolio_user` is the RESUME prompt, live, right now — and this lane did not change it.** Ground-truthed from the PRIMARY SOURCE rather than by comparing the two live rows (which can only say they are the same, never which is wrong). Live `GET /api/prompts`, Actions run **32435525197**: `resume_user` and `portfolio_user` are byte-identical, sha256 `4b4af848…`, 29,068 chars; control `ats_user` differs. The zap export in this repo says which one is the mistake: node **289877661** ("Update Resume/Portfolio Fields", user_message 29,069) vs node **299599701** ("Copy: …", user_message 7,712). Live `portfolio_user` matches node 289877661 — the RESUME node — whitespace-normalised, 29,060-char common prefix; against node 299599701 it diverges after 329 chars. **Consequence on every production packet:** Call 2 is asked for `###` sections (42 markers, no mention of JSON) and parsed with `parseAgentJson`, so it returns nothing usable, the portfolio and cover letter fall back to Call 1, and a 16,000-token call is spent to achieve that. **The remedy is not authoring** — the correct text is checked in at `docs/zap-289877647/prompts/17-copy-update-resume-portfolio-fields-prompt.md`. It is deferred because `POST /api/prompts` rewrites live document generation for the real owner and that is an owner decision, not a drive-by. `duplicatePromptPairs` now NAMES the condition in `warnings` so it cannot stay silent. NOTE the near-miss: `resume_system` == `portfolio_system` is also byte-identical and is **CORRECT** — both zap nodes carry the same 331-char system_message — so a check over all prompts would have cried wolf on every run. | The run completed, four documents were produced, and every one of them looked filled. | Owner sign-off, then `prompts-load-file.yml` with node 17's `user_message`. Re-run `GET /api/prompts` and confirm the two digests now DIFFER. |
| D30 | **DISPUTED: the cold AC agent asked that EVERY `pass:false` path be non-2xx; I kept 2xx for the completed-but-not-clean path.** Its criterion 2 (`docs/qc-evidence/P7-D11-D12-D13-ACS.md`, branch `claude/qc-p7-acs`) says a caller reading only the status must see `pass:false` as failure on all four paths. My reason for not doing it: `POST /api/pipeline/run` COPIES GOOGLE FILES and is not idempotent, so a non-2xx on a run that actually delivered a packet invites a retrying client to run it again — and P7-ACCEPTANCE's own note is that any retry design must be traced against X5 (render once) first or a retry MULTIPLIES the D13 orphans. `runOutcome` is a pure exported function precisely so this decision can be changed in one place with a test. **This is an owner/architecture call, not a defect either way.** | Both readings are defensible and the tests pass under mine. | An owner decision on whether a degraded-but-delivered run is an HTTP failure. |
| D31 | **The `api-test.yml` assertion is narrower than the cold ACs asked for, deliberately, and the gaps are real.** Not done: (a) `ok:false` at 200 — excluded because `appRemediation` uses it for a REFUSAL and firing on it is the cry-wolf failure, but `packetBuildAll` uses the same key for a genuine failure, so the key is overloaded at the SOURCE and the honest fix is to split it there; (b) a top-level `error` key at 200 — same class, seven call sites in `appPackets.ts` alone; (c) a 3xx still counts as success (`status >= 400`); (d) **`omit_auth: true` is permanently red** — the one input that exists to test the REJECT path exits 1 when the API correctly returns 401, so its result is uninterpretable, and D12's fix makes the vehicle strictly more misleading until it is fixed. Each needs an `expect_status`-style input. | The workflow now goes red on `pass:false`, which reads as "the vehicle is honest now". | Anyone touching `api-test.yml` next. (d) first — it is the one that makes a correct result unreadable. |
| D32 | **`renderArtifact` still orphans one Drive file on EVERY rebuild.** D13 closed the FAILED-build leak; the superseded file is a different one. `doc_url` is overwritten and the previous id is returned as `supersededDocUrl` for the caller to record — nothing deletes it, and nothing persists it to a queryable store either, so the orphan population remains unenumerable. Recorded here because closing D13 without saying so is exactly what this file exists to prevent (the cold ACs' criterion 33 asks for this row by name). | `deleteDriveFile` now exists and D13 reads as closed, so Drive cleanup looks handled. | An owner decision on reaping superseded files (P3-24 / D-9), or a table to record them in. |
| D33 | **The four Drive ids and the `dev@enterpriseds.io` sender are STILL hardcoded in the legacy MT-XX and diag routes** — `mt03..mt06`, `mt12`, `mt18`, `mt19`, `diagDrive`, `diagTemplates`, `diagFolders`, `mt07`, `mt08`, `mt21`, `mt33`, `mt47`. Deliberately untouched: they are the dev-console test harness, not the product, and "fix all consumers" was applied to the two paths that ship documents. `mt19.ts:133` also still has the raw `Promise.all(docJobs)` D13 shape. `H:no-second-id-copy` exempts them by name. | P7 item 8 is marked done and `H:no-second-id-copy` is green — it scans `pipeline.ts` and `appPackets.ts` only. | Whenever the MT harness is next touched, or when a second tenant makes the sender wrong everywhere. |
| D34 | **`H33` FAILS on `main` at `8e4c46c`** — "apply is allowlisted but a caller CAN send it — the allowlist is hiding a real gap". Confirmed pre-existing, not caused by this lane: it fails identically with every change on this branch stashed (56 pass / 1 fail vs 64 pass / 1 fail). It belongs to the X2 regen-reachability lane that owns H33/D7. | The suite is otherwise green, so a single red case reads as the lane that is currently running. | The X2 lane, or whoever next runs `npm test` on main and assumes their change broke it. |
| D35 | **`git stash` is SHARED ACROSS WORKTREES and cost this lane its uncommitted work once.** Five lanes are writing into one checkout with `.claude/worktrees/*` worktrees; the stash lives in the shared `.git`, so a `git stash push` here and a concurrent stash operation elsewhere raced: three uncommitted files (`appPackets.ts`, `api-test.yml`, and 313 lines of `hardening.test.mjs`) vanished from this worktree, and another lane's `figureEcho.ts` changes appeared in it (saved to `/tmp/foreign-figureEcho.patch` and discarded from here, NOT from wherever it belongs). Everything already committed survived. **The rule: never `git stash` in a multi-lane checkout. To test with a change removed, copy the file to `/tmp` and copy it back.** | `git stash pop` reported nothing and the tree looked plausible afterwards. | Every lane, now. This is a working-practice hazard, not a code defect. |

## P8.4 / comparison dimensions (`claude/qc-p8-4-dimensions`)

| # | Not done | What makes it look done | Trigger |
|---|---|---|---|
| D21 | **`comparison_dimension` is NOT in `SCHEMA_SQL` or `EXPECTED_TABLES`.** D1/H11's rule is that a new table is declared in `schema.ts` so a migration gap is a failing test rather than a 500. The lane was instructed not to edit `schema.ts` (another lane owns it mid-fix), so the table is created by `ensureDimensionTable` only. Consequence: `diag/pg-migrate` will not list it in its completeness report, and **H11 does not guard it**. | The table is created correctly on every request that touches it — the ensure-path works, and the store is proven against a populated database. Nothing 500s, so nothing signals the omission. | The `schema.ts` lane lands. Add the DDL to `SCHEMA_SQL`, the name to `EXPECTED_TABLES`, and the name to H11's hand-maintained array (all three — the third is the one that gets forgotten). |
| D22 | ~~`experience.years_leadership` unreachable in `checkAgainstFacts`~~ **FIXED** on `claude/qc-live-defects`. Selection is no longer by catalogue POSITION: `FactDef.refines` declares that `years_leadership` narrows `years_total`, and `selectFactDef` drops any matching def that another matching def refines. The gate change is the one D22 predicted and it is the intended one — a leadership requirement answered only by a total-years figure is now `unknown` (which PROPOSES the leadership fact) instead of falsely `satisfied`, and a recorded-but-short leadership figure WARNS with its own arithmetic. **`coverable` membership is unchanged** — `ownedByFacts` already absorbed both `unknown` and resolved verdicts, so the same rows leave `coverable` either way; what changed is WHICH fact the verdict is about. `H41` was rewritten from pinning the shadow to guarding the fix (undeclared strict-subset relations measured over a 25-line corpus, plus the behavioural half), `H41b` and `H43` added, and `checks.test.mjs`'s fixture — which asked for LEADERSHIP years, recorded only TOTAL years and asserted a pass — corrected. **NOT verified live**: no before/after count on real data, because the sandbox cannot reach the database. | — | — |
| D23 | **Org size and budget are never graded from their numbers.** `checkAgainstFacts` does arithmetic for `unit: 'years'` only (`ownerFacts.ts:116`); `people` and `usd` fall through to `unknown`. Those rows report `numeric_verdict: 'unavailable'` and refuse to grade from the figure — correct, and it means two of the eight named dimensions can only ever be graded from evidence-row overlap, never from "60+ engineers vs 62". | The rows render, they are two-sided (the owner's recorded figure IS shown), and they carry a reason. It reads as a considered result rather than a missing comparator. | A comparator for `people` and `usd`. It belongs in `ownerFacts` beside `demandedNumber`, not in `dimensions.ts` — a second numeric parser is a second answer. Same blast radius as D22, so pair them. |
| D24 | **No Settings control for the dimension set.** AC6 asks for one in the existing Settings shell. The ROUTE exists and round-trips (`GET/POST /api/app/dimension-prefs`, proven against a populated database), and the JD step names the set and its source — but the owner cannot change it without calling the API. | The comparison card says "Seeded dimension set for engineering - you have not changed it yet", which reads like an affordance exists somewhere. | P8 UI close-out. The control is a checkbox list per role family; `TemperatureSettings` (`Settings.jsx`) is the shape to copy — it is ~55 lines against the same store. |
| D25 | **Nothing about P8.4 is verified live.** The engine, the store and the surface are proven by execution in the sandbox (372 api + 172 app tests, a populated-database migration, 28 reinstated defects). Nothing has been run against the Function App, the real database or the deployed SPA. | Every test passes and the evidence is unusually concrete, which reads as verification. It is verification of the MECHANISM, not of the product. | Landing on `main`. Then: `api-test.yml` `POST /api/app/opportunity/<id>/evidence` (builds the comparison), `db-query.yml` for the AC34 audit query, and `ui-verify.yml` on `#/packet/<id>/jd` with `EXPECT` on the four column headings and `EXPECT_ABSENT` on "posting lines". |

## Live but unconfirmed

| # | Not done | What makes it look done | Trigger |
|---|---|---|---|
| D17 | ~~Nothing deployed~~ **DONE for four lanes** — P8.7, P8.2, P8.3 and X2-regen are on `main` at `ed19230` and deployed green. **P3 is the only lane still unlanded.** | — | — |
| D18 | ~~P0.3 dark accent pills at 1.90:1~~ **CLOSED — the claim does not reproduce.** Re-measured `.px-btn-accent` and `.px-btn-dark` with `getComputedStyle` in Chromium on `main` at `ee3d989`: **9.05:1 light, 5.94:1 dark**. Both pass. The buttons take `--surface-brand-default` with `--text-on-brand` on top, not `--proto-accent`, so the token this row named was never the one they read. Either P8.7 fixed it or the original 1.90:1 was measured against something else — I did not establish which, and the row is closed on the measurement, not on the history. **The real accent defect was a different one and is now fixed** (see D26). | The row named a plausible token and a specific ratio, and neither was checked again for two phases. | — |
| D19 | **Stored evidence is never re-validated on read.** `requirement_evidence.record_sha256` is written and served but never recomputed; `requirementsGet`'s `stale` flag covers the posting hash only. After the owner edits a MasterContext block the JD payload keeps serving the old quote at the old offsets. | The excerpt renders normally and is a true substring of what the record USED to say. | a design decision about reading the profile on every requirements GET |
| D20 | **A remediation pass's package mutation is not atomic with its ledger row.** The ledger inserts and escalations now share one transaction (all-or-nothing), but `packet.pkg_json` is updated per pass BEFORE the ledger is written, so a failure between them leaves a package this run modified with no row recording it. The handler now REPORTS that state explicitly (`packageMutated: true, ledgerWritten: false`) instead of returning a bare 500 — reporting it is not fixing it. | The endpoint returns a structured 500 that reads like a handled error, and re-running is safe, so the window is easy to mistake for closed. | `evaluateArtifact` (P8.3's file) giving up ownership of its own `begin`/`commit`, which is what makes a transaction spanning a whole pass possible |
| D21 | **`escalation.ats_term_id` has no foreign key and no writer.** The column exists and the `check (requirement_id is not null or ats_term_id is not null)` names it, so it reads as a supported second target. Every escalation this lane writes sets `requirement_id`. | The column is in the schema and in a CHECK. | P1.2 publishing a `term_library` version worth pointing at |
| D22 | **P3-09 and P3-33 are contradictory and neither acceptance document reconciles them.** P3-09 says no `remediation_loop` row may have `n > max_passes` for a packet; P3-33 requires a resolved escalation to continue the ledger at `max(n)+1`. The implementation follows P3-33, so a second run on a 4-pass ceiling writes n=5..8. This is an unresolved criterion conflict, not an implementation slip. | Both criteria are marked sandbox-verifiable, and the loop passes every test written for it. | an owner decision on whether the ceiling is per-run or per-packet |

## Contrast

| # | Not done | What makes it look done | Trigger |
|---|---|---|---|
| D26 | ~~Eight of nine status tones below 4.5:1~~ **CLOSED, fixed and deployed** on `main` at `ee3d989`. Seven token values; `accent` was 2.90:1 in dark — the worst text in the app, labelling the QC rail's swap decisions. `app/test/browser/run-tones.mjs` now sweeps all nine tones in both themes, reading `TONE` from `shell.jsx` rather than a copy, and fails loudly rather than silently when blinded. | — | — |
| D27 | **`px-small` is tertiary ink at 2.56:1 in light** — measured by the P8.6 lane, NOT re-measured by me and NOT fixed. It is a shared utility class, so the blast radius is different from the tones and needs its own trace. | The tones sweep is green and reads as "contrast is handled". It covers the nine pill tones and nothing else. | Now — same method, `run-tones.mjs` is the template |
| D28 | **Nothing sweeps contrast outside the nine tones.** Buttons, links, inputs, temperature chips and every ad-hoc `color:` in a `.jsx` style prop are unmeasured. D27 is one instance of this. | Two contrast guards exist (brand surfaces, and now tones), so the area looks covered. | When D27 is picked up — widen the sweep rather than adding a third one-off |
