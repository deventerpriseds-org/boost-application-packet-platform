# Prototype coverage — how much of the Packet QC prototype's UI is in the live app

**Measured:** 2026-08-25 · **Branch:** `claude/render-interaction-states` · **Base commit:**
`d8aec3c` (local `HEAD` == `origin/main`, verified with `git fetch origin` before measuring).

**This is a measurement, not a design document.** Nothing under `app/src` or `api/src` was
touched. No file other than this one was modified.

> **Written incrementally, section by section.** If this document ends mid-table, everything above
> the cut is complete and citable; nothing below it was started.

---

## 0. What was measured, and against what

**The denominator is the prototype**, read directly: `docs/qc-evidence/qc/*.jsx` (2,410 lines
across `assets.jsx` 487, `packet.jsx` 573, `evidence.jsx` 447, `shell.jsx` 131, `assist.jsx` 123,
`data.js` 649) plus `Packet QC Prototype.html`. Rows are grouped by `SPEC.md` §4.1–§4.12 so every
row is citable against the handover spec.

**A row = one user-visible control, panel, chip, state or affordance.** Not a string, not a demo
value, not a colour token. `UI-GAP-REGISTER.md:41-44` already establishes why string equality is
the wrong instrument, and `COMPONENT-INVENTORY.md:690-700` measured that **26 of 40** of that
register's printed `resume` rows are SafetyIQ demo values rather than components.

**Verdict vocabulary** (inherited from `COMPONENT-INVENTORY.md:24-25` so the two documents
compare):

| Verdict | Meaning |
|---|---|
| **BUILT** | The element and its states exist in the app. A rename is not a gap. Requires a `file:line`. |
| **PARTIAL** | The element exists; named states, colours, transitions or mounts are missing. Requires a `file:line`. |
| **ABSENT** | No counterpart in the app. Never asserted from a single-file grep (see method limits, §14). |
| **DELIBERATE** | Diverges or is omitted **by a recorded decision**. Cited to `PULL-CANDIDATES.md`, `.claude/actions.md` or `.claude/DEFERRED.md`. **Counted separately — this is not a gap.** |
| **NOT-IN-PROTOTYPE** | The prototype does not implement it either (a dead handler, a mockup-only affordance). Cannot be "missing from the app". Excluded from the denominator. |
| **OUT-OF-SCOPE** | `SPEC.md:8` §8 or §4.12 says *do not build it*. Excluded from the denominator. |

---

## 1. PRIOR MEASUREMENTS — what was already counted, and by what instrument

The owner is right that this was measured before. **Three prior instruments exist, and none of
them measured what was asked for now.** Stating that up front is the point: the delta in §13 is
only honest against the one that is comparable.

### 1a. `UI-GAP-REGISTER.md` — a rendered-TEXT diff (2026-08-23)

Generated by `scripts/compare-ui.mjs`. Renders both sides and diffs text + font weight.

| Step | app% | Panels missing | Controls missing |
|---|---:|---:|---:|
| `jd` | 266% | 27 | 3 |
| `resume` | 76% | 62 | 4 |
| `cover` | 65% | 22 | 3 |
| `portfolio` | 83% | 24 | 4 |
| `video` | 76% | 2 | 0 |
| `qc` | 85% | 22 | 4 |
| `send` | 70% | 6 | 1 |
| **total** | | **165** | **19** |

**Why this is NOT the number the owner asked for.** Its own file says so twice. `app%` is *"the
app's rendered text length as a fraction of the prototype's — a blunt proxy … not a quality
score"* (`UI-GAP-REGISTER.md:16-18`), and the 165/19 count is explicitly *not* a backlog
(`:31-44`): 65% of its `resume` rows are demo values, 15% are measurement strings whose component
is built, 15% are built-and-renamed components, and one row measures a **banned** string that can
never close. Its own conclusion: *"of the 40 printed rows, ZERO name a component that is absent
and buildable"* (`COMPONENT-INVENTORY.md:733`).

### 1b. `COMPONENT-INVENTORY.md` — a COMPONENT + STATE diff (2026-08-24) — **the comparable one**

21 components, resume/asset step only (SPEC §4.4 + §4.5 + part of §4.9). Verdicts read from
`**App status:**` lines at `COMPONENT-INVENTORY.md:49,76,109,140,167,203,243,266,293,326,364,390,417,463,489,532,571,592,604,621,642`:

| Verdict | Sections | Count | Share of 21 |
|---|---|---:|---:|
| BUILT | §5 §7 §8 §9 §12 §13 §15 §17 §19 §20 | **10** | 47.6% |
| PARTIAL | §2 §6 §10 §11 §16 §21 | **6** | 28.6% |
| ABSENT | §1 §14 | **2** | 9.5% |
| BLOCKED (no data source) | §3 §4 §18 | **3** | 14.3% |

**Headline as it stood on 2026-08-24: 10 of 21 fully built (47.6%); 16 of 21 present in some form
(76.2%).** Scope: the asset step only. It says nothing about §4.1, §4.2, §4.3, §4.8, §4.10 or §4.11.

### 1c. `.claude/accuracy-log.md` — three wrong ABSENT claims, all dated **2026-08-25** (today)

Read before writing a single verdict in this document, because all three are the exact failure
mode this measurement is exposed to:

1. *"the term library blocks Row 11"* — **FALSE.** `requirement.model_keyword` was flowing end to
   end and already rendering at `PostingAnalysis.jsx:401`. A code comment was read as a
   constraint instead of the data being traced.
2. *"there is no Undo control in the field margin"* — **FALSE**, ~90% was already built.
   `CorrectionRow` is imported at `AssetBlocks.jsx:42` from `QcRail.jsx` and mounted in the margin.
   A single-file grep cannot see a control that lives in an imported component.
3. *"the `Reword it` toggle is open owner-blocked work"* — **FALSE.** Origin was the prototype,
   not the owner, and it had already been decided against with `Ask for a reword` shipped as the
   substitute (`.claude/actions.md:2947-2951`).

**Consequence for this document:** every ABSENT verdict below was checked against the mounting
file's *import list* and a repo-wide grep, and every "open" row was reconciled against
`.claude/actions.md` and `.claude/DEFERRED.md` before being written down.

### 1d. Why 1b's number moved before this measurement even began

`PULL-CANDIDATES.md` (written 2026-08-25, after the inventory) records **Row 11 Phase A and Phase
B as shipped** — PC-1, PC-3 and PC-5 all read *"Shipped in: Row 11 Phase A/B"*. Those are the
keyword chips: precisely §3 / §4 / §18, the three the inventory called `BLOCKED` on the term
library. So the 47.6% figure is known-stale before a line of code is read. §13 quantifies it.

---

## 2. SPEC §4.1 — JD analysis, "Extracted from this posting"

**Prototype:** `qc/packet.jsx:27-166` (`ParsedBlocks`). **App:** `PostingAnalysisCard`,
`app/src/screens/PostingAnalysis.jsx:384-514`, logic in `app/src/postingAnalysis.js`.
Import list read (`PostingAnalysis.jsx:18-28`): `shell.jsx` (Pill, Overlay), `postingAnalysis.js`
(pure logic), `highlight.js`. **No component is imported that could carry an evidence panel.**

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.1-1 | Card heading "Extracted from this posting" | `packet.jsx:157` | BUILT | `PostingAnalysis.jsx:428` — "Posting analysis - the source". Renamed. |
| 4.1-2 | Explanatory sub-line | `packet.jsx:161` | BUILT | `PostingAnalysis.jsx:430-436` |
| 4.1-3 | `See where each one is answered →` (deep-link to QC) | `packet.jsx:159` | ABSENT | The card's only header control is `Show as tabs/columns` (`:439-442`). The compare card has `See the lines this was built from` (`:204`) but it points the other way — compare → extraction, not extraction → QC. |
| 4.1-4 | Three-tab strip, one list at a time | `packet.jsx:116-123` | BUILT | `PostingAnalysis.jsx:485-493`, `role="tablist"` / `role="tab"` — better than the prototype's bare divs. |
| 4.1-5 | Per-tab count `n/m` | `packet.jsx:120` | PARTIAL | `PostingAnalysis.jsx:490` renders `({t.count})` — a **single** total, not `covered/total`. Deliberate: `:400-403` says attaching a coverage number to `model_keyword` "made a suggestion look like a measurement". |
| 4.1-6 | Count coloured green when complete, red when not | `packet.jsx:120` | ABSENT | No colour on the tab count at `:490`. |
| 4.1-7 | Responsibilities list | `packet.jsx:124` | BUILT | `PostingAnalysis.jsx:409-411`, `Group` at `:262-283` |
| 4.1-8 | Requirements list | `packet.jsx:125-132` | BUILT | `PostingAnalysis.jsx:412-419` |
| 4.1-9 | MUST HAVE / NICE TO HAVE sub-headers | `packet.jsx:127,129` | BUILT | `PostingAnalysis.jsx:414,417` — `Must-have` / `Nice-to-have` |
| 4.1-10 | Sub-header `n/m evidenced` | `packet.jsx:63-72` | PARTIAL | `PostingAnalysis.jsx:270-275` renders `{split.total}` plus a **kind_source** split, not an *evidenced* ratio. A different and better-sourced number; the evidenced ratio itself does not render. |
| 4.1-11 | Row: the posting line **verbatim** | `packet.jsx:41` | BUILT | `PostingAnalysis.jsx:231-237` — a `<blockquote>` with char offsets, plus an explicit paraphrase branch at `:243-248` the prototype has no equivalent for. |
| 4.1-12 | Row: requirement chip in a 150–210px right column | `packet.jsx:40,43` | PARTIAL | The chip is BUILT (`PostingAnalysis.jsx:220-222`, `reqChipLabel`) but sits **above** the line, not in a right column — the row is a single flow column (`:216-228`). |
| 4.1-13 | Row: competency spelled out beside the id (R7) | `packet.jsx:43` via `ReqChip` | BUILT | `PostingAnalysis.jsx:225-227` — `{r.competency \|\| 'competency unassigned'}` |
| 4.1-14 | Row: status dot, green covered / red open | `packet.jsx:39` | ABSENT | No dot in `RequirementRow` (`:214-256`), and no coverage colour anywhere in the file. |
| 4.1-15 | Row: `evidenced — show the line` link | `packet.jsx:45` | ABSENT | Nothing in `RequirementRow` expands. Repo-wide `grep -rn evidence app/src` returns only `Settings.jsx:1558` (owner facts) and comments — no requirement-evidence renderer. |
| 4.1-16 | Row: `no evidence found` in red | `packet.jsx:46` | ABSENT | Same. |
| 4.1-17 | Expanded row: verbatim profile excerpt | `packet.jsx:52` | ABSENT | Same. |
| 4.1-18 | Expanded row: the excerpt's named source | `packet.jsx:53` | ABSENT | Same. |
| 4.1-19 | Expanded row: optional supporting note (`ev.extra`) | `packet.jsx:54` | ABSENT | Same. |
| 4.1-20 | Expanded row: `Where it is used →` | `packet.jsx:56` | ABSENT | Same. |
| 4.1-21 | Keywords tab: one row per library term | `packet.jsx:87-100` | DELIBERATE | `D:term-library-off-by-owner-decision` (`.claude/DEFERRED.md:191`) — *"we will leave the library turned off for now"*. The app renders `KeywordLibraryState` (`:288-296`) which says so **in words derived from the checks row**, not hardcoded. |
| 4.1-22 | Keywords tab: term status dot | `packet.jsx:90` | DELIBERATE | Same decision. |
| 4.1-23 | Keywords tab: green/red term chip | `packet.jsx:93` | DELIBERATE | Same decision. The app's neutral chip (`KeywordChips`, `:304-317`) deliberately refuses the green/red pair — `:319-326` records that rendering an unmeasured list green "lent the unmeasured half the credibility of the measured one". |
| 4.1-24 | Keywords tab: `placed` / `nothing to place it on` | `packet.jsx:94` | DELIBERATE | Same decision. |
| 4.1-25 | Keywords tab: the posting phrase the term matched | `packet.jsx:96` | DELIBERATE | Same decision. |
| 4.1-26 | Keywords tab: `answers M2 · appears 2× in the ad` | `packet.jsx:97` | DELIBERATE | Same decision. |
| 4.1-27 | Keywords tab: `n of m placed` tally | `packet.jsx:102` | DELIBERATE | Same decision — `keywordLibraryState()` prints the state instead. |
| 4.1-28 | Footer naming the library (`ENG-LEAD v4`, 1,840 terms, sources) | `packet.jsx:103` | DELIBERATE | Same decision — there is no published version to name. |
| 4.1-29 | Model-suggested terms earning no score credit | `packet.jsx:104` | BUILT | `PostingAnalysis.jsx:345-386` (`ModelKeywords`) — *"excluded from ATS scoring"*, plus a `parsed` / `from_run` / `thin` provenance split the prototype has no equivalent for. |
| 4.1-30 | Keyword list 2-up ≥ 1040px, 1-up below | SPEC §3 | BUILT | `postingAnalysis.js` `keywordColumns()` / `keywordGridTemplate()`, rendered `PostingAnalysis.jsx:364-367` as `data-qc-cols`. |
| 4.1-31 | Legend row, always present | `packet.jsx:163` | BUILT | `PostingAnalysis.jsx:502-511` |
| 4.1-32 | Three-column layout behind `PARSED_LAYOUT` | `packet.jsx:25,136-152` | BUILT | `PostingAnalysis.jsx:389-392,439-442,575-582` — and it is a **persisted user preference** (`ee_posting_columns`), not a code constant, per the repo's "no hardcoded config" rule. Better than the prototype. |

**§4.1 tally — 32 rows:** BUILT **13** · PARTIAL **3** · ABSENT **8** · DELIBERATE **8**.
Against the 24 rows that are not owner-deferred: **13 BUILT (54%), 16 present in some form (67%).**

**The one cluster that matters:** rows 4.1-14 → 4.1-20, seven consecutive rows, are one feature —
*the evidence expansion*. SPEC §4.1's acceptance says *"every 'evidenced' claim can be expanded to
a quote plus source"*. That does not render anywhere in the app. Note this is **not** the same as
"the evidence spine does not work" — `.claude/DEFERRED.md:131` records `D:evidence-resolves-nothing`
as **CLOSED** (1 → 6 evidenced on the live Trinnex posting). The data exists; the JD step does not
read it.

---

## 3. SPEC §4.2 — JD analysis, "Posting vs your profile"

**Prototype:** `qc/packet.jsx:169-213` (`ProfileCompare`). **App:** `ProfileCompareCard`,
`PostingAnalysis.jsx:124-208`, logic `postingAnalysis.js:65-150`.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.2-1 | Four fit **cards** (Responsibilities / Must-have / Nice-to-have / ATS keywords) | `packet.jsx:174-188` | ABSENT | No card grid in `ProfileCompareCard`. Grep for a `{n} of {d}` card across `app/src`: only `Swipe.jsx:211` and `PostingAnalysis.jsx:105` (a per-**row** sub-line, not a card). |
| 4.2-2 | Card: `n of m` big number | `packet.jsx:179-180` | PARTIAL | Survives only as `{r.covered} of {r.total} line(s)` inside each comparison row, `PostingAnalysis.jsx:104-106`. |
| 4.2-3 | Card: graded verdict word | `packet.jsx:182` | BUILT | `fitLabel()` `postingAnalysis.js:78-81`, rendered `PostingAnalysis.jsx:100-103`. |
| 4.2-4 | Card: `Missing: <named>` when incomplete | `packet.jsx:184` | PARTIAL | The app names the reason (`compareNote`, `:110-119`) and splits `weak` into `Nothing found` vs `Falls short` (`postingAnalysis.js:66-81`) — a **deliberate improvement**, recorded in that comment — but it does not enumerate the missing items by name. |
| 4.2-5 | Comparison table, 4 columns | `packet.jsx:191-194` | BUILT | `COMPARE_COLUMNS` `postingAnalysis.js:150`, rendered `PostingAnalysis.jsx:186-192`. Exported so a test asserts the headings **are** the spec's. |
| 4.2-6 | Row: dimension label | `packet.jsx:197` | BUILT | `PostingAnalysis.jsx:77` |
| 4.2-7 | Row: what the posting asks | `packet.jsx:198` | BUILT | `PostingAnalysis.jsx:79-86` — plus a `Model paraphrase - not the employer's wording` disclosure the prototype lacks. |
| 4.2-8 | Row: what the profile evidences | `packet.jsx:199` | BUILT | `PostingAnalysis.jsx:88-95` — plus a named source per cell. |
| 4.2-9 | Row: qualifying note | `packet.jsx:199` | BUILT | `PostingAnalysis.jsx:110-119`, and **mandatory** for every moderate/weak/ungraded row. |
| 4.2-10 | Row: fit dot + coloured label | `packet.jsx:200-203` | BUILT | `PostingAnalysis.jsx:97-104`, `FIT_COLOR` `postingAnalysis.js:84-89` |
| 4.2-11 | Grading thresholds (≥0.99 strong, ≥0.7 moderate) | SPEC §4.2 | BUILT | Graded server-side and read off the stored row — `PostingAnalysis.jsx:66-69`: *"nothing here is computed in the browser"*. |
| 4.2-12 | Scope copy — "graded against your stored profile only" | `packet.jsx:208` | BUILT | `COMPARE_SCOPE_NOTE`, rendered `PostingAnalysis.jsx:199-203` |
| 4.2-13 | `See how the assets answer these →` button | `packet.jsx:209` | PARTIAL | `PostingAnalysis.jsx:204-207` renders a button, but it goes to the extracted lines (`See the lines this was built from`), not to the QC step. |
| 4.2-14 | Responsive: 4-col ≥ 900px, 1-col below | `packet.jsx:171` `useWide(900)` | BUILT | `COMPARE_WIDE_MIN = 900`, `compareColumns()` `postingAnalysis.js:127-141`, rendered as `data-qc-cols` at `:180`. |
| 4.2-15 | The empty / unresolved state | *(prototype has none)* | NOT-IN-PROTOTYPE | `comparisonState()` `postingAnalysis.js:97-122` gives four distinct states; `compareEmpty` `:163-167`. App-only. Excluded from the denominator. |

**§4.2 tally — 14 rows (row 15 excluded):** BUILT **10** · PARTIAL **3** · ABSENT **1** · DELIBERATE **0**.
**10 BUILT (71%), 13 present in some form (93%).**

The app also carries four surfaces the prototype has no counterpart for — dimension-set provenance
(`compareSetSource`, `:139-149`), staleness (`compareStale`, `:154-160`), the strong/moderate/weak
summary (`compareSummary`, `:171-179`), and `not_applicable` as a first-class neutral grade. These
are **not** counted as coverage; they are noted because a coverage percentage measured only in one
direction reads as though the app is strictly behind, and on this section it is not.

---

## 4. SPEC §4.3 — ATS analysis modal

**Prototype:** `qc/packet.jsx:287-301` (the modal frame) + `:322-355` (`sidePanel`, its contents).
**App:** `KeywordTallyOverlay`, `PostingAnalysis.jsx:562-604`; opened by `MatchEstimateButton`
(`:610-621`), mounted `PacketBuilder.jsx:941,963,1029`.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.3-1 | The header score is the affordance that opens it | `packet.jsx:515-519` | BUILT | `MatchEstimateButton` `PostingAnalysis.jsx:610-621`; wired `PacketBuilder.jsx:963,1029`. A real `<button>`, not the prototype's clickable div. |
| 4.3-2 | Sub-label naming what it opens | `packet.jsx:518` | BUILT | `PostingAnalysis.jsx:619` — `model estimate · keywords ↗`. Renamed per the naming rule at `:9-13`. |
| 4.3-3 | Modal frame with backdrop + ✕ | `packet.jsx:287-301` | BUILT | `Overlay ... variant="modal"` `PostingAnalysis.jsx:566-568` |
| 4.3-4 | Modal title + packet subtitle | `packet.jsx:293-294` | BUILT | `PostingAnalysis.jsx:566-567` |
| 4.3-5 | The score, large, colour-graded | `packet.jsx:327` | BUILT | `PostingAnalysis.jsx:576-578` — and `not run yet` rather than a fabricated 0. |
| 4.3-6 | Placed / open keyword chips | `packet.jsx:330-331` | DELIBERATE | `D:term-library-off-by-owner-decision`. `KeywordLibraryState` `:581-584` states it. |
| 4.3-7 | `n/m placed` | `packet.jsx:333` | DELIBERATE | Same. |
| 4.3-8 | `Auto-optimize resume` button | `packet.jsx:335` | BUILT | `PostingAnalysis.jsx:587-589` — `Rebuild every asset from this posting`. Renamed and broader. |
| 4.3-9 | QC Summary block header | `packet.jsx:341` | ABSENT | The overlay has no QC summary. |
| 4.3-10 | Composite match / requirements / keywords / seniority bars (`ScoreBlock compact`) | `packet.jsx:342` | ABSENT | No `ScoreBlock` equivalent renders inside the modal. |
| 4.3-11 | Per-asset gate rows with `GateBadge` | `packet.jsx:344-349` | ABSENT **in this surface** | `GateBadge` exists (`AssetGateDrawer.jsx:45-76`) and IS rendered per asset — on the artifact card (`PacketBuilder.jsx:184`) and in the QC rail — but not inside the tally modal. This is a **relocation**, not a missing component. |
| 4.3-12 | `Open QC →` button | `packet.jsx:350` | PARTIAL | `PostingAnalysis.jsx:590` offers `Go to the resume step`; there is a QC step (`PacketBuilder.jsx:108`) but the modal does not link to it. |
| 4.3-13 | **Any navigation out closes the modal first** | `packet.jsx:345,347,350` (`setPanelOpen(false)` on every exit) | PARTIAL | `Overlay` owns close; `onGoResume` / `onBuildAll` are supplied by `PacketBuilder.jsx:941-960` — needs a runtime check to confirm the dismiss ordering (see §14). |
| 4.3-14 | Model-keyword list inside the modal | *(prototype has none)* | NOT-IN-PROTOTYPE | `PostingAnalysis.jsx:586`. App-only; excluded. |

**§4.3 tally — 13 rows (row 14 excluded):** BUILT **6** · PARTIAL **2** · ABSENT **3** · DELIBERATE **2**.
Against the 11 non-deferred rows: **6 BUILT (55%), 8 present in some form (73%).**

---

## 5. SPEC §4.4 — Asset steps (artifact card + asset header)

**Prototype:** `qc/packet.jsx:215-275` (`ArtifactCard`) + `qc/assets.jsx:195-282` (`AssetHeader`).
**App:** `ArtifactCard` `PacketBuilder.jsx:148-360`; `DistributionMeter` `AssetBlocks.jsx:256-330`.

> **This section and §4.5 are where `COMPONENT-INVENTORY.md` (2026-08-24) measured. Every row below
> was RE-READ against today's code rather than copied**, and eight of its verdicts have changed —
> see §13.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.4-1 | Card: asset title | `packet.jsx:224` | BUILT | `PacketBuilder.jsx:176` |
| 4.4-2 | Card: subtitle | `packet.jsx:225` | BUILT | `PacketBuilder.jsx:177` |
| 4.4-3 | Card: **gate badge** (`97 · 3 to review`) | `packet.jsx:227` | **BUILT — CHANGED from PARTIAL** | `PacketBuilder.jsx:184` `<GateBadge result={qcResult} compact />`. Inventory §21 called this "not rendered on this step"; it is now. Component `AssetGateDrawer.jsx:45-76`. |
| 4.4-4 | Card: status pill | `packet.jsx:228` | BUILT | `PacketBuilder.jsx:178` |
| 4.4-5 | `Open Google Doc ↗` | `packet.jsx:239` | BUILT | `PacketBuilder.jsx:231-233` |
| 4.4-6 | `Open Slides ↗` | `packet.jsx:239` | BUILT | `PacketBuilder.jsx:232` |
| 4.4-7 | `Copy tracked link` | `packet.jsx:240` | BUILT | `PacketBuilder.jsx:237-247` |
| 4.4-8 | Those three are real **buttons**, `nowrap` | `packet.jsx:239-240` | PARTIAL | They are `<a>` and `role="button"` spans (`:231,237`). The a11y hole the register flagged is closed (`role` + `tabIndex` + key handler, `:235-241`) but they are not `px-btn` buttons. Cosmetic; recorded as PARTIAL, not ABSENT. |
| 4.4-9 | The asset's field blocks | `packet.jsx:245` | BUILT | `PacketBuilder.jsx:200-204` → `AssetBlocks` |
| 4.4-10 | `Approve` | `packet.jsx:255` | BUILT | `PacketBuilder.jsx:301-303` |
| 4.4-11 | **`Approve` disabled when the gate fails** | `packet.jsx:219,255` | **BUILT — CHANGED from ABSENT** | `PacketBuilder.jsx:163` (`gateBlocks`), `:301-303` `disabled={gateBlocks}` + a `title` saying why. Inventory (`COMPONENT-INVENTORY.md:655-658`) recorded this as ABSENT. |
| 4.4-12 | `Regenerate` | `packet.jsx:256` | BUILT | `PacketBuilder.jsx:331-333` |
| 4.4-13 | Asset-level `Ask for a change` | `packet.jsx:257` | **BUILT — CHANGED from ABSENT** | `PacketBuilder.jsx:290-292` (`List Tweaks`, `PACKET_HOOKS.assetAsk`), box at `:344+`. Inventory build-order row 6 called this "the last unbuilt item"; it is built, on the same `artifactAiEdit` route with no `section`, exactly as the triage instructed. |
| 4.4-14 | Deep-linking `n to fix — <title> →` when the gate blocks | `packet.jsx:261-271` | PARTIAL | The counts render (`GateBadge` `AssetGateDrawer.jsx:70-71`, `N to fix` / `N to review`) but the card's badge is mounted **without** `onClick` (`PacketBuilder.jsx:184`), so the count does not open the first failing field. R5 ("every count deep-links") is unmet on this surface. |
| 4.4-15 | `Generate draft` / `Generate script` (todo) | `packet.jsx:252` | BUILT | `PacketBuilder.jsx:276-278` |
| 4.4-16 | `Reopen` (approved) | `packet.jsx:260` | BUILT | `PacketBuilder.jsx:337` |
| 4.4-17 | Asset header, **collapsed by default** | `assets.jsx:197,213-226` | BUILT | `AssetBlocks.jsx:265-330`; default from `ASSET_BODY_DEFAULT_OPEN` (`PacketBuilder.jsx:163`, named constant, not a bare `false`). |
| 4.4-18 | Collapsed row: `n corrected` | `assets.jsx:219` | BUILT | `AssetBlocks.jsx:288-290` (`BLOCK_HOOKS.meterCorrected`) |
| 4.4-19 | Collapsed row: `n to fix` | `assets.jsx:220` | BUILT | `AssetBlocks.jsx:298-300` (`meterToFix`) |
| 4.4-20 | Collapsed row: `n to review` | `assets.jsx:221` | BUILT | `AssetBlocks.jsx:304-306` (`meterToReview`) |
| 4.4-21 | Collapsed row: `n your call` | `assets.jsx:222` | BUILT | `AssetBlocks.jsx:320-322` (`meterYourCall`) |
| 4.4-22 | Collapsed row: `Show ▾` disclosure | `assets.jsx:224` | BUILT | `AssetBlocks.jsx` meter toggle — a real `role="button"` disclosure, better than the prototype's whole-row div. |
| 4.4-23 | Expanded: plain-language summary sentence | `assets.jsx:236-241` | BUILT | `AssetBlocks.jsx:279-282` (`meterSummary`), sourced from `meterModel` (`assetBlocks.js`) rather than fabricated. |
| 4.4-24 | Expanded: `Must-haves` counter | `assets.jsx:243` | PARTIAL | One combined `Posting lines placed` stat renders (`AssetBlocks.jsx:226-234`, `BLOCK_HOOKS.stat`); the per-kind split does not. Needs per-kind denominators on `GET /app/opportunity/{id}/requirements` (returns `total` only) — an endpoint extension. |
| 4.4-25 | Expanded: `Responsibilities` counter | `assets.jsx:244` | PARTIAL | Same. |
| 4.4-26 | Expanded: `Nice-to-haves` counter | `assets.jsx:245` | PARTIAL | Same. |
| 4.4-27 | Expanded: `ATS keywords` counter | `assets.jsx:246` | DELIBERATE | `D:term-library-off-by-owner-decision`. `UNKNOWN_TERMS_NOTE` (`assetBlocks.js:456`, pushed at `:570`) renders the honest unknown instead of a fabricated 0. |
| 4.4-28 | Expanded: the open-items list (fail / open / warn / soft) | `assets.jsx:248-261` | **BUILT — CHANGED from PARTIAL** (per field) | `AssetBlocks.jsx:709-728` (`BLOCK_HOOKS.fieldFindings`) renders every severity with `SEV_LABEL` + offenders + expected. Inventory build-order row 3 said "the renderer is not [built]". **Relocated deliberately** — the findings render in each field's margin rather than in one list on the header, which is the same relocation §4.5's `Changes made` took. |
| 4.4-29 | List row: `Go to field →` | `assets.jsx:257` | PARTIAL | Deep links exist and work (`focusField` `PacketBuilder.jsx`→`AssetBlocks.jsx:1020-1021`, `useScrollToFocus` `:913`), but they are driven from the QC rail, not from a `Go to field →` control on the asset header. |
| 4.4-30 | `Already corrected in this asset` group | `assets.jsx:262-276` | BUILT (relocated) | `AssetBlocks.jsx:694-697` — `Corrected for you`, `CorrectionRow ... inField`. Relocation recorded in `UI-GAP-REGISTER.md:98-101` as a **deliberate deviation** (one correction rendered twice is what the corrections guard prevents). |
| 4.4-31 | Corrected row: `Review →` | `assets.jsx:272` | BUILT | Carried by `CorrectionRow` (`QcRail.jsx`, imported `AssetBlocks.jsx:44`). |
| 4.4-32 | `Nothing to review on this asset.` | `assets.jsx:279` | **BUILT — CHANGED from ABSENT** | `AssetBlocks.jsx:309-316` (`BLOCK_HOOKS.meterClear`), guarded on a **loaded** result so "clear" and "never checked" cannot look alike. Inventory build-order row 8. |
| 4.4-33 | `open` / "Needs your answer" severity | `assets.jsx:248` | DELIBERATE | `assetGate.js:78-87` refuses to mint this bucket from state the app does not have. Inventory: *"ABSENT by design and correctly so"*. |

**§4.4 tally — 33 rows:** BUILT **24** · PARTIAL **7** · ABSENT **0** · DELIBERATE **2**.
Against the 31 non-deferred rows: **24 BUILT (77%), 31 present in some form (100%)** — every row
has at least a partial counterpart.

---

## 6. SPEC §4.5 — Field blocks

**Prototype:** `qc/assets.jsx` — `Marked` `:8-22`, `Rule` `:24-30`, `KeyChip` `:34-49`,
`KeyDetail` `:53-88`, `EchoTrail` `:92-144`, `ReqChip` `:151-164`, `ReqLegend` `:166-178`,
`FieldList` `:283-303`, `PickList` `:305-...`, `AssetBlock`/`AssetDocView`.
**App:** `AssetBlocks.jsx:396-905` + `assetBlocks.js`.
Import list read (`AssetBlocks.jsx:30-45`) — `CorrectionRow` comes from `QcRail.jsx:44`,
`useScrollToFocus`/`focusRingStyle` from `focusRing.js:45`, `markRuns` from `highlight.js:41`.
**No verdict below rests on a single-file grep.**

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.5-1 | One card per merge field | `assets.jsx` `AssetDocView` | BUILT | `AssetBlocks.jsx:1014-1022`, `BLOCK_HOOKS.field` at `:880` |
| 4.5-2 | Two columns, 250px provenance margin | SPEC §3, §4.5 | BUILT | `AssetBlocks.jsx:883` — `gridTemplateColumns: wide ? 'minmax(0,1fr) 250px' : '1fr'`. **Exactly 250px.** |
| 4.5-3 | Stacks below the breakpoint | SPEC §3 (`≥ 1080px`) | PARTIAL | Stacking works (`:883`, `useWideRef` `:909`) but the threshold is **700px**, not the spec's 1080. A deliberate-looking choice with no recorded decision — flagged, not counted as absent. |
| 4.5-4 | Slot / field name | `assets.jsx` | BUILT | `AssetBlocks.jsx:533` `fieldLabel(row.merge_field)` |
| 4.5-5 | Live rule computed from the string (`56 words · 55–60 words`) | `assets.jsx:24-30` | BUILT | `observedFor()` / `targetFor()` (`assetBlocks.js`), rendered `AssetBlocks.jsx:546-558`. Both halves unit-matched. |
| 4.5-6 | **Rule STATE COLOUR** (pass/warn/fail) | `assets.jsx:24-30` | **BUILT — CHANGED from PARTIAL** | `AssetBlocks.jsx:546-551` — `data-qc-sev={fieldSev}`, `SEV_COLOR[fieldSev]`. Colour comes from the field's check rows through the same `severityFor` split the rail uses, and **no finding = no colour** so an unmeasured field cannot read as passing. Inventory build-order row 4. |
| 4.5-7 | Real merge-field name in mono | `assets.jsx` | BUILT | `AssetBlocks.jsx:561-562` (`BLOCK_HOOKS.fieldSlot`, `ui-monospace`) |
| 4.5-8 | Body: prose shape | `assets.jsx` | BUILT | `shapeOf()` `assetBlocks.js:144-151` → `prose`; `BlockBody` `AssetBlocks.jsx:574` |
| 4.5-9 | Body: list shape, `original → final` rows | `assets.jsx:283-303` (`FieldList`) | BUILT | `ListBody` in `AssetBlocks.jsx`; `shapeOf()` → `list`. Inventory §13: *"BUILT, with a different and better-sourced status column."* |
| 4.5-10 | Body: `unchanged` on a kept row | `assets.jsx:299` | BUILT | Same component; the app's status column is sourced from `swap_decision.action`. |
| 4.5-11 | Body: pipe block, monospace | `assets.jsx` | BUILT | `shapeOf()` `assetBlocks.js:147` → `pipe` |
| 4.5-12 | Body: **pick-list** (`type: 'select'`, checkboxes + per-item requirement) | `assets.jsx:305-...` (`PickList`) | ABSENT | `shapeOf()` (`assetBlocks.js:144-151`) returns only `static` / `pipe` / `list` / `prose` — **no `select` shape exists.** Verified by reading the function, not by grep. Portfolio-only in the prototype, so no resume impact; needs per-item candidacy on the insertions payload. |
| 4.5-13 | **Keyword hits highlighted in the draft text** | `assets.jsx:8-22` (`Marked`) | **BUILT — CHANGED from ABSENT** | `markRuns` imported `AssetBlocks.jsx:41`, called `:446`, `Marked` at `:446-460`, phrases assembled `:486`. Inventory build-order **row 1**, the top-ranked gap and "the unlock for four other rows" — it is built. |
| 4.5-14 | Posting echoes highlighted (pale tan + underline) | `assets.jsx:8-22` | BUILT | Same pass — `:484` *"Both treatments go through markRuns in ONE pass"*; classes `HIGHLIGHT_CLASS.postingEcho` (`highlight.js`), tokens `theme.css`. |
| 4.5-15 | Hovering a margin row lights its phrase in the text | `assets.jsx:159` | **BUILT — CHANGED from ABSENT** | `onMouseEnter={() => setActiveWording(phrase)}` `AssetBlocks.jsx:750`, and for a keyword chip `:794`; `active` compared by identity at `:451-456` with `HIGHLIGHT_ACTIVE_CLASS`. |
| 4.5-16 | Hovering the TEXT lights its margin row (the reverse direction) | `assets.jsx:19` | NOT-IN-PROTOTYPE | `qc/assets.jsx:19` has **no `onMouseEnter` handler on the mark** — the prototype's text→margin direction is CSS `:hover` styling only, with nothing wired to the margin. Cannot be "missing from the app". Excluded from the denominator. |
| 4.5-17 | `Show original` on every field | `assets.jsx` | BUILT | `AssetBlocks.jsx:616-622`, unconditional; `originalState()` decides the three states. |
| 4.5-18 | `Show original` distinguishes `identical` (template not merged per packet) | SPEC §4.5 `sameAsBefore` | **BUILT — CHANGED from PARTIAL** | `AssetBlocks.jsx:580-590` — three states (`changed` / `identical` / `none`) decided in `assetBlocks.js` so `node --test` can hold them. Inventory build-order row 7 called the old "before this posting" heading a false claim; it is fixed. |
| 4.5-19 | Per-field `Ask for a change` | `assets.jsx:475-...` (`AskBox`) | BUILT | `AssetBlocks.jsx:632-641` (`BLOCK_HOOKS.askChange`), renamed `List Tweaks` — inventory §15: *"and it is the more honest name"*. |
| 4.5-20 | Ask box scoped to one field, says so | `assets.jsx` | BUILT | `AssetBlocks.jsx:649` — *"This rewrites `{merge_field}` only."* |
| 4.5-21 | Margin: origin `Written for this posting · pass n` | `assets.jsx` | BUILT | `AssetBlocks.jsx:678-686` |
| 4.5-22 | Margin: origin `Template · same in every packet` | `assets.jsx` | BUILT | `AssetBlocks.jsx:683` |
| 4.5-23 | Margin: open/review items for THIS field | SPEC §4.5 | **BUILT — CHANGED from PARTIAL** | `AssetBlocks.jsx:709-728` — `Open on this field`, all severities, offenders, expected. Same row as 4.4-28. |
| 4.5-24 | Margin: `Changes made` — struck original → new wording | `assets.jsx:92-121` | BUILT | `CorrectionRow` (imported `:44` from `QcRail.jsx`) mounted `:696` with `inField`. Heading `Corrected for you` `:694`. |
| 4.5-25 | Margin: the reason for the change | `assets.jsx` | BUILT | Carried by `CorrectionRow`. |
| 4.5-26 | Margin: `Undo` | `assets.jsx:159` | BUILT | Carried by `CorrectionRow`; client export `revertCorrection` (`api.js:205`). **This is `accuracy-log.md`'s second wrong-ABSENT — confirmed BUILT by reading the import list.** |
| 4.5-27 | Margin: `Suggest something different` | `assets.jsx` | BUILT | Carried by `CorrectionRow` (the suggest box). |
| 4.5-28 | Margin: `Keywords placed` chips | `assets.jsx:34-49` (`KeyChip`) | **BUILT — CHANGED from BLOCKED** | `AssetBlocks.jsx:780-810` (`BLOCK_HOOKS.keywordChips` / `keywordChip`), headed `Keywords for this line` (`:782`), fed by `proposedKeywordsForRow(reqs)` `:477` from `requirement.model_keyword`. Labelled `proposed`, not "placed" — the honest label, since these are model-proposed rather than library-scored. **This is `accuracy-log.md`'s first wrong-ABSENT, now shipped.** |
| 4.5-29 | Chip: `≈` prefix for a reworded term | `assets.jsx:34-49` | DELIBERATE | `PULL-CANDIDATES.md` **PC-3** — *"'reworded' is undecidable, not merely unsourced"*. Recorded, reversible, not a gap. |
| 4.5-30 | Chip: match grade (Exact / Reworded / Loose) | SPEC §4.6 via the chip | DELIBERATE | Same PC-3. |
| 4.5-31 | Chip click opens the keyword detail panel | `assets.jsx:53-88` (`KeyDetail`) | **BUILT — CHANGED from BLOCKED** | `AssetBlocks.jsx:472-478` (`openKeyword`, `proposedKeywordDetail`), panel `:818-830`. One open at a time, local to the field — same single-value semantics as the prototype. |
| 4.5-32 | Margin: `Posting lines answered` requirement chips | `assets.jsx:151-164` (`ReqChip`) | BUILT | `AssetBlocks.jsx:844` (singular/plural, heading `Posting line(s) answered`); the chip component is `ReqChip` at `:163-170` |
| 4.5-33 | Chip colour split covered / open | `assets.jsx:151-164` | DELIBERATE | Inventory §7 (`COMPONENT-INVENTORY.md:249-255`): the state cannot occur in the app's model — a chip in a field's margin **is** a line that field answers, so there is no `open` case to paint. |
| 4.5-34 | Requirement legend (`M1–M5` / `D1–D4` / `N1–N3`) | `assets.jsx:166-178` | BUILT | `AssetBlocks.jsx:188` (`BLOCK_HOOKS.reqLegend`) — filters to the kinds actually present, better than three fixed rows. |
| 4.5-35 | Margin: the prompt's own reason for the field | `assets.jsx` | BUILT | `AssetBlocks.jsx:519-528` (`rationales` / `reason`), with an explicit honest-absence branch. |
| 4.5-36 | Margin: a keyword the field CLAIMS but does not contain | `assets.jsx` (§18) | **BUILT — CHANGED from BLOCKED** | `keywordPresence()` `assetBlocks.js:364`, called `AssetBlocks.jsx:482`, rendered `:803-810` as `not in this text`. Shipped, and recorded as reversible in `PULL-CANDIDATES.md` **PC-1**. |
| 4.5-37 | Margin: `Wording kept from the posting` | `assets.jsx:122-144` | BUILT | `AssetBlocks.jsx:730-770`; label via `checkLabel('posting_wording_kept')` (`assetGate.js:161`). |
| 4.5-38 | The `rewording` state on a kept phrase (strike-through + toggle) | `assets.jsx:122-144` | DELIBERATE | `.claude/actions.md:2947-2951` — decided against, substitute shipped as `Ask for a reword` (`AssetBlocks.jsx:499-502,761,765`). **`accuracy-log.md`'s third wrong-ABSENT: origin is the PROTOTYPE, not the owner.** |
| 4.5-39 | Static blocks show their actual template text | SPEC §4.5 | BUILT | `shapeOf()` → `static` when `!row.generated` (`assetBlocks.js:144-146`); rendered by `BlockBody`. |
| 4.5-40 | Static blocks show `{{merge field}}` placeholders inline | SPEC §4.5 | ABSENT | Nothing in `AssetBlocks.jsx` or `assetBlocks.js` renders a `{{…}}` placeholder; static bodies print the stored text. Checked both files and the import list; `BlockBody` (`:574`) has no placeholder branch. |
| 4.5-41 | Focus ring + scroll-to-field on the asset card | SPEC §4.9 applied here | **BUILT — CHANGED from PARTIAL** | `useScrollToFocus` `AssetBlocks.jsx:913`, `focusRingStyle(focused)` `:885`, wired `:1020-1021`. Inventory build-order **row 2**. |
| 4.5-42 | The ring clears after ~2.2s | `screens/` notes | DELIBERATE | `PULL-CANDIDATES.md` **PC-4** — the persistent ring is a deliberate choice; SPEC says nothing about ring timing and the repo has twice removed vanishing affordances for the same reason. |
| 4.5-43 | Chips deduped / near-duplicates collapsed | *(prototype: exact ids)* | DELIBERATE | `PULL-CANDIDATES.md` **PC-5** — ordered by requirement `seq`, deduped by EXACT string only; collapsing is a similarity judgement this repo reserves for ranking. |

**§4.5 tally — 42 rows (4.5-16 excluded as NOT-IN-PROTOTYPE):** BUILT **32** · PARTIAL **1** ·
ABSENT **2** · DELIBERATE **7**.
Against the 35 non-deferred rows: **32 BUILT (91%), 33 present in some form (94%).**

---

## 7. SPEC §4.6 — Keyword detail panel

**Prototype:** `qc/assets.jsx:53-88` (`KeyDetail`). **App:** `AssetBlocks.jsx:472-478, 818-830`.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.6-1 | The panel opens from a chip, one at a time | `assets.jsx:53-56` | BUILT | `AssetBlocks.jsx:472-478` — `openKeyword` is a single value, not a set, exactly as the prototype. |
| 4.6-2 | Panel: the term | `assets.jsx:61` | BUILT | `AssetBlocks.jsx:820` |
| 4.6-3 | Panel: ✕ closes | `assets.jsx:63` | BUILT | `AssetBlocks.jsx:818-830` |
| 4.6-4 | Panel: **match quality** (Exact / Reworded / Loose) | `assets.jsx:62` | DELIBERATE | `PULL-CANDIDATES.md` **PC-3**. `matchesEntry` needs a published `term_library_entry`; more fundamentally *"'reworded' is undecidable"*. |
| 4.6-5 | Panel: the posting phrase (`Posting says "…"`) | `assets.jsx:65` | BUILT | `Verbatim` `AssetBlocks.jsx:204-208` — `Posting says: "…"`, always-on rather than hover-gated. |
| 4.6-6 | Panel: the note explaining the wording choice | `assets.jsx:66` | BUILT | `AssetBlocks.jsx:826-830` — *"A model reading this posting proposed this keyword for the line below. Nothing has…"* |
| 4.6-7 | Panel: **what it displaced** ("Took the place of X in Skills 1") | `assets.jsx:67` | DELIBERATE | PC-3. `swap_decision` stores `from_label → to_label` and PC-3 names it as the honest future source. |
| 4.6-8 | Action: `Put back "<original>"` | `assets.jsx:70-74` | PARTIAL | The **capability** exists — `CorrectionRow`'s `Undo` (`QcRail.jsx:578-580` → `POST /app/correction/{id}/revert`) and the Swaps/Original-vs-final tab — but the panel itself does not carry a `Put back` button. Relocated, not absent. |
| 4.6-9 | Action: `Swap for another skill…` (a select from the skill bank) + `Swap` | `assets.jsx:76-83` | ABSENT | No skill-bank select renders anywhere on the asset step. Checked `AssetBlocks.jsx` and its import list plus `assetBlocks.js`; the only `<select>` on this surface is none. |
| 4.6-10 | Action: `Drop it, leave the line open` | `assets.jsx:84-85` | ABSENT | No equivalent control. |
| 4.6-11 | Each action is phrased as an assistant request stating the coverage consequence | `assets.jsx:72,82,85` | ABSENT | The three escape hatches are what carry this; without them there is nothing to phrase. Note `AssetBlocks.jsx:495-502` **does** use exactly this pattern for the reword case (`seedAskReword`), so the mechanism exists and is not wired to this panel. |
| 4.6-12 | `not in this text` on a chip whose keyword the draft lacks | *(prototype has none)* | NOT-IN-PROTOTYPE | App-only, `AssetBlocks.jsx:803-810`, recorded as reversible in PC-1. Excluded. |

**§4.6 tally — 11 rows (row 12 excluded):** BUILT **5** · PARTIAL **1** · ABSENT **3** · DELIBERATE **2**.
Against the 9 non-deferred rows: **5 BUILT (56%), 6 present in some form (67%).**

---

## 8. SPEC §4.7 — Inline "Ask for a change"

**Prototype:** `qc/assets.jsx` (`AskBox`). **App:** `AssetBlocks.jsx:632-660`.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.7-1 | Opens **under the field** | `assets.jsx` `AskBox` | BUILT | `AssetBlocks.jsx:632-660`, inside the field card. |
| 4.7-2 | Labelled `ASK FOR A CHANGE · <FIELD>` | SPEC §4.7 | BUILT (renamed) | `AssetBlocks.jsx:634` `data-qc-field={row.merge_field}`; heading text is `List Tweaks`. |
| 4.7-3 | Textarea | SPEC §4.7 | BUILT | `AssetBlocks.jsx:632-660` |
| 4.7-4 | `Scoped to this field only.` | SPEC §4.7 | BUILT | `AssetBlocks.jsx:649` — *"This rewrites `{merge_field}` only. Anything auto-corrected in it can no longer be undone."* Says more than the prototype. |
| 4.7-5 | Cancel | SPEC §4.7 | BUILT | `AssetBlocks.jsx:637` (the control toggles to `Cancel`) |
| 4.7-6 | Send | SPEC §4.7 | BUILT | `AssetBlocks.jsx:632-660` → `api.aiEditArtifact` |
| 4.7-7 | Confirms in place on send | SPEC §4.7 | PARTIAL | The route writes and the block reloads; a distinct in-place confirmation line is not evidenced by reading. Needs a runtime check (§14). |
| 4.7-8 | Forwards to the assistant | SPEC §4.7 | ABSENT | There is no assistant panel to forward to — see §4.11 below. The app applies the edit directly instead. |
| 4.7-9 | Asset-level equivalent for artifacts with no merge fields | *(prototype: `packet.jsx:257`)* | BUILT | `PacketBuilder.jsx:290-292` + `:344+` — exists precisely because the intro video has no merge fields. |

**§4.7 tally — 9 rows:** BUILT **7** · PARTIAL **1** · ABSENT **1** · DELIBERATE **0**.
**7 BUILT (78%), 8 present in some form (89%).**

---

## 9. SPEC §4.8 — QC & evidence step

**Prototype:** `qc/evidence.jsx:336-388` (`QCStep`) + `AttentionList` `:92-121`, `CoverageView`
`:123-195`, `SwapsView` `:197-245`, `PassesView` `:247-264`, `ChecksView` `:266-289`,
`ReviewerView` `:291-334`, `ScoreBlock` `:30-64`, `OpenItems` `:66-90`.
**App:** `QcRail` `screens/QcRail.jsx:660-880`, logic `qcRail.js`. Mounted `PacketBuilder.jsx:895-905`.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.8-1 | Header: composite match | `evidence.jsx:30-64` | PARTIAL | `QcRail.jsx:775-786` renders a headline **and refuses to fabricate one** — `railHeadline` prints `why` instead of a number when parts have no source, and `:773-775` says a packet-wide composite would be invented. A deliberate honesty divergence, but the number itself does not render, so PARTIAL. |
| 4.8-2 | Header: must-have coverage | `evidence.jsx:30-64` | PARTIAL | Component bars render at `QcRail.jsx:788-802` with `not measured` where a part has no source. |
| 4.8-3 | Header: pass count | `evidence.jsx` | BUILT | The `Remediation loops` tab, `LoopsTab` `QcRail.jsx:356-399`, `RAIL_TABS` `qcRail.js:86`. |
| 4.8-4 | Header: `n to fix, n to review` | `evidence.jsx` | BUILT | `QcRail.jsx:710-722` (`QC_HOOKS.toFix`, `toReview`), **plus** `never checked` and `corrected for you` as separate third and fourth numbers (`:723-738`) — R4 applied harder than the prototype does. |
| 4.8-5 | Header: per-asset gate chips | `evidence.jsx` | BUILT | `QcRail.jsx:744-768` — label, gate pill, `n to fix`, `n to review`, clickable into the drawer. |
| 4.8-6 | **`Done for you`** list, on the page | `evidence.jsx:92-121` | BUILT | `ChangeLog` `QcRail.jsx:612-650`, mounted `:808` with the comment *"ON THE PAGE (SPEC 4.8) - not behind a tab and not behind a search."* |
| 4.8-7 | Done-for-you row: `Corrected for you` + what changed + why | `evidence.jsx` | BUILT | `CorrectionRow` `QcRail.jsx:489-600` |
| 4.8-8 | Done-for-you row: `Change it` | `evidence.jsx:108` | PARTIAL | The row carries `Undo` (`:578-579`) and `Review →` (`:557`); a separate "Change it" that seeds a rewrite request is not there. The capability is the field's `List Tweaks`. |
| 4.8-9 | Done-for-you row: `Review →` | `evidence.jsx:109` | BUILT | `QcRail.jsx:557` |
| 4.8-10 | **`Needs a decision`** list, on the page | `evidence.jsx:92-121` | ABSENT | After `ChangeLog` the app goes straight to the tab strip (`QcRail.jsx:810-830`). The open items live in the **Checks** tab and the drawer, never as a page-level "things the run could not settle" list. Verified by reading the whole render body `:702-860`, not by grep. |
| 4.8-11 | Attention ordering fail → open → warn → fixed → soft | SPEC §5 | PARTIAL | `railAttention` / `attentionSplit` exist (`qcRail.js`, `assetGate.js`) and severity ordering is encoded, but with no page-level attention list there is no surface where the full ordering renders. |
| 4.8-12 | `Open field →` deep link on each open item | `evidence.jsx:113` | BUILT | `onGoToField` threaded `PacketBuilder.jsx:901` → `QcRail.jsx:837-838` → `AssetBlocks.jsx:1020` focus ring. R5 is met on this path. |
| 4.8-13 | Questions offer `Answer` | `evidence.jsx:80,112` | DELIBERATE | Requires the `open` severity, which `assetGate.js:78-87` refuses to mint from state the app does not have (same decision as 4.4-33). |
| 4.8-14 | Tab: **Coverage** — posting line by line, expandable | `evidence.jsx:123-195` | BUILT | `CoverageTab` `QcRail.jsx:246-299`, three cards, one per requirement class, each with its own closed/total (`:240-245`). |
| 4.8-15 | Coverage: keyword tally beside each line | `evidence.jsx` | DELIBERATE | `D:term-library-off-by-owner-decision`. |
| 4.8-16 | Tab: **Swaps** — original → final | `evidence.jsx:197-245` | BUILT | `CompareTab` `QcRail.jsx:300-355`, tab labelled `Original vs final` (`qcRail.js:85`). |
| 4.8-17 | Swaps: covering keyword | `evidence.jsx` | DELIBERATE | Same library decision. |
| 4.8-18 | Swaps: verbatim posting quote | `evidence.jsx` | BUILT | `CompareTab` renders the stored quote column; `qcRail.js:324` guards it — *"a quote gets read as evidence"* — with a test greping for the column name. |
| 4.8-19 | Swaps: the reason | `evidence.jsx` | BUILT | Same table (`rationale`). |
| 4.8-20 | Swaps: `Undo this` | `evidence.jsx:232` | PARTIAL | Correction undo exists (`QcRail.jsx:578-579`); a per-**swap** undo on this tab does not. |
| 4.8-21 | Swaps: `Ask why` | `evidence.jsx:233` | ABSENT | No equivalent control on `CompareTab` (`:300-355`, read in full). |
| 4.8-22 | Tab: **Passes** — what each loop closed / what remained / where it halted | `evidence.jsx:247-264` | BUILT | `LoopsTab` `QcRail.jsx:356-399` |
| 4.8-23 | Tab: **Checks** — grouped by name, observed vs target, named offenders | `evidence.jsx:266-289` | BUILT | `ChecksTab` `QcRail.jsx:400-439`; `CheckRow` `:182-239`; offenders `AssetGateDrawer.jsx:93-100`. |
| 4.8-24 | Tab: **Review** — blind second model: grade, agreement, prompt version, citations, critique | `evidence.jsx:291-334` | BUILT | `ReviewTab` `QcRail.jsx:440-480` |
| 4.8-25 | A picked requirement filters the other tabs | `evidence.jsx:378` | BUILT | `QcRail.jsx:820-830` — `filtered to #{seq}` + a `clear` affordance, keyboard-reachable. |

**§4.8 tally — 25 rows:** BUILT **14** · PARTIAL **5** · ABSENT **2** · DELIBERATE **4**.
Against the 21 non-deferred rows: **14 BUILT (67%), 19 present in some form (90%).**

---

## 10. SPEC §4.9 — Per-asset QC drawer

**Prototype:** `qc/evidence.jsx:390-447` (`QCDrawer`). **App:** `AssetGateDrawer.jsx` (573 L).

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.9-1 | Right drawer over the current step | `evidence.jsx:390-405` | BUILT | `AssetGateDrawer.jsx` via the shared `Overlay` primitive (`shell.jsx`) — escape, focus trap, scroll lock, close-on-navigation, none of which the prototype has. |
| 4.9-2 | Header names the asset | `evidence.jsx:400-404` | BUILT | `AssetGateDrawer.jsx` header |
| 4.9-3 | Header repeats the gate badge | `evidence.jsx:403` | BUILT | `GateBadge` `AssetGateDrawer.jsx:45-76` |
| 4.9-4 | Tab: Fields | `evidence.jsx:409` | BUILT | `TABS` `AssetGateDrawer.jsx:123-129` (`blocks`), `FieldsTab` `:135-178` |
| 4.9-5 | Tab: Checks | `evidence.jsx:409` | BUILT | `AssetGateDrawer.jsx:125,182-201` |
| 4.9-6 | Tab: Swaps | `evidence.jsx:409` | BUILT | `AssetGateDrawer.jsx:202-265` (`compare`) |
| 4.9-7 | Tab: Review | `evidence.jsx:409` | BUILT | `AssetGateDrawer.jsx:266-279` |
| 4.9-8 | Tab: Match | `evidence.jsx:409` | BUILT | `AssetGateDrawer.jsx:128,280-320` — and *"A component with no source shows the server's prose for WHY it is missing, never a 0"*. |
| 4.9-9 | Footer: `Approve` disabled + `n to fix · <first title> · +n more` | `evidence.jsx:425` | BUILT | `footerFor()` `assetGate.js:264-300`; `fail` → `{disabled:true, headline:'Blocked', reason:'n blocking finding(s)…'}` `:283-286`. |
| 4.9-10 | Footer: `Approve with note` + `n to review · records who approved and why` | `evidence.jsx:433` | BUILT | `footerFor()` `assetGate.js:287-296` — `warn` → `Approve with exceptions`, `needsReason`, and the override records `by` + `reason`. |
| 4.9-11 | Footer: plain `Approve` | `evidence.jsx:437` | BUILT | `footerFor()` default branch. |
| 4.9-12 | Footer: `Ask for a change` | `evidence.jsx:440` | PARTIAL | Not in the drawer footer; the equivalent lives on the card (`PacketBuilder.jsx:290`) and per field (`AssetBlocks.jsx:632`). Relocated. |
| 4.9-13 | Deep link scrolls to the target field and outlines it | `evidence.jsx:390` | BUILT | `useScrollToFocus` `AssetGateDrawer.jsx:12,140`, `focusRingStyle` `:158`; a deep link naming a section also **returns to the Blocks tab** (`:380-386`) — a case the prototype does not handle. |
| 4.9-14 | The outline clears after ~2s | `evidence.jsx` | DELIBERATE | `PULL-CANDIDATES.md` **PC-4**. |
| 4.9-15 | `unchecked` as a distinct footer state | *(prototype has none)* | NOT-IN-PROTOTYPE | `assetGate.js:268-271`. App-only; excluded. |
| 4.9-16 | Advisory-mode override with a recorded reason | *(prototype has none)* | NOT-IN-PROTOTYPE | `assetGate.js:272-282`. App-only; excluded. |

**§4.9 tally — 14 rows (15, 16 excluded):** BUILT **12** · PARTIAL **1** · ABSENT **0** · DELIBERATE **1**.
Against the 13 non-deferred rows: **12 BUILT (92%), 13 present in some form (100%).**
**§4.9 is the best-covered section in the spec.**

---

## 11. SPEC §4.10 — Review & send

**Prototype:** `qc/packet.jsx:429-475`. **App:** `PacketBuilder.jsx:907-930`.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.10-1 | Per-asset list | `packet.jsx:433-439` | BUILT | `PacketBuilder.jsx:911-923` |
| 4.10-2 | Per-asset **gate badge** on the list | `packet.jsx:436` | ABSENT | `PacketBuilder.jsx:918-921` renders the label and the status `Pill` only. `GateBadge` is imported into this file (`:15`) and used at `:184` — so this is a **missing mount, not a missing component**, and `qcEntries` is already in scope at `:719`. |
| 4.10-3 | Per-asset status pill | `packet.jsx:437` | BUILT | `PacketBuilder.jsx:919` |
| 4.10-4 | Packet gate card: `n items to fix across m assets` | `packet.jsx:452-455` | ABSENT | Nothing renders it. `packetReadiness()` (`qcRail.js:831-847`) computes the packet gate and a `contradiction` sentence, and `readiness` is assigned at `PacketBuilder.jsx:719` — the data is there; this card is not. |
| 4.10-5 | "Sending stays locked until each one is fixed or the decision is recorded" | `packet.jsx:456` | PARTIAL | `PacketBuilder.jsx:927` says *"Approve all artifacts above to unlock sending."* — the same idea, sourced from `p.status === 'ready'` (`:715`) rather than from the live fail list. |
| 4.10-6 | One row per failing item | `packet.jsx:458-467` | ABSENT | No per-item rows on this step. |
| 4.10-7 | Failing row: `Open field →` | `packet.jsx:465` | ABSENT | Same. |
| 4.10-8 | `Nothing blocks sending` when the list empties | `packet.jsx:445-448` | PARTIAL | `PacketBuilder.jsx:925-926` shows `Go to outreach →` when `ready`. The affordance appears; the sentence and the derivation-from-the-live-fail-list do not. |

**§4.10 tally — 8 rows:** BUILT **2** · PARTIAL **2** · ABSENT **4** · DELIBERATE **0**.
**2 BUILT (25%), 4 present in some form (50%). This is the weakest section in the spec**, and
notably the cheapest to close — every input (`qcEntries`, `GateBadge`, `packetReadiness`,
`onGoToField`) is already imported into the very file that renders it.

---

## 12. SPEC §4.11 — Assistant · §4.12 — Prototype-only comparison mode

### §4.11 Assistant

**Prototype:** `qc/assist.jsx` (123 L), docked `packet.jsx:540-547`.

Repo-wide grep for an assistant surface on this screen: `grep -rniE "assist" app/src` returns
`postingAnalysis.js:454` (a comment recording the ≥1440px rule), `AssetBlocks.jsx:495` (a comment
saying the prototype's "Ask assistant" **seeds the field's own ask box instead**), and
`Call.jsx:222-225` — a separate live-coach screen, not this panel. Import lists of
`PacketBuilder.jsx`, `AssetBlocks.jsx`, `QcRail.jsx` and `AssetGateDrawer.jsx` were all read: none
imports an assistant component.

| # | Prototype element | Proto ref | Verdict | App citation / note |
|---|---|---|---|---|
| 4.11-1 | Docked right column ≥ 1440px | `packet.jsx:540-544` | ABSENT | No such panel. |
| 4.11-2 | Collapses to a card with `Open assistant` | `assist.jsx` | ABSENT | — |
| 4.11-3 | Floating panel below 1440px | `packet.jsx:547` | ABSENT | — |
| 4.11-4 | Scope selector (This packet / This asset / My profile) | `assist.jsx` | ABSENT | — |
| 4.11-5 | Quick actions (Put back an original · Undo a swap · Shorten to fit · Say why · Keyword is wrong) | `assist.jsx` | PARTIAL | Three of the five exist as **scoped in-place controls** rather than assistant prompts: undo (`QcRail.jsx:578`), reword/say-why (`AssetBlocks.jsx:499-502`), keyword-is-wrong (the PC-1 `not in this text` chip). "Put back an original" and "Shorten to fit" have no control. |
| 4.11-6 | Replies list the exact merge fields they would touch | `assist.jsx` | ABSENT | — |
| 4.11-7 | Keep / Revert / Re-run QC on a reply | `assist.jsx` | PARTIAL | `Undo` (`QcRail.jsx:578`) and re-run checks (`AssetGateDrawer.jsx:417` `runChecks`) exist as first-class controls; there is no reply to attach them to. |
| 4.11-8 | Caveat when a change will be reverted by the next run (the omission list) | `assist.jsx` | ABSENT | — |
| 4.11-9 | **Every field-level action seeds this panel** | SPEC §4.11 | DELIBERATE (architectural) | `AssetBlocks.jsx:495` records the substitution explicitly: the field's own ask box **is** the seeded surface. This is the app's deliberate architecture — a scoped edit route (`artifactAiEdit`) instead of a conversational panel — not an oversight. It is recorded in code, however, not in `actions.md`; **the owner has not signed off on replacing §4.11 wholesale.** |

**§4.11 tally — 9 rows:** BUILT **0** · PARTIAL **2** · ABSENT **6** · DELIBERATE **1**.
Against the 8 non-deferred rows: **0 BUILT (0%), 2 present in some form (25%).**

> **This is the single largest un-built surface in the spec, and it is the one row in this document
> whose classification I am least confident about.** It is either (a) a deliberate architectural
> replacement, in which case eight rows leave the denominator and every headline below rises, or
> (b) a real gap. The evidence for (a) is a code comment, and `accuracy-log.md`'s first entry is
> exactly the failure of treating a code comment as a decision. **Ask the owner which it is** — it
> moves the headline by about four points on its own.

### §4.12 Prototype-only comparison mode

**OUT-OF-SCOPE by the spec's own words.** `SPEC.md` §4.12: *"This is a review aid for the spec —
do not build it."* §8 repeats it. `UI-GAP-REGISTER.md:22-26` records that counting the mode pill
and the `UI SPEC` badge *"inflated an earlier version of this file with work nobody should do."*

| # | Element | Verdict |
|---|---|---|
| 4.12-1 | `Current app` mode | OUT-OF-SCOPE |
| 4.12-2 | `With QC layer` mode | OUT-OF-SCOPE |
| 4.12-3 | `Highlight additions` outlines | OUT-OF-SCOPE |
| 4.12-4 | The bottom mode pill / `ProtoControls` | OUT-OF-SCOPE |
| 4.12-5 | The `<New>` annotation wrapper | OUT-OF-SCOPE |

**5 rows, all excluded from the denominator.** Counting them would be the exact error the register
already made once.

---

## 13. THE HEADLINE, AND THE DELTA

### 13a. The number

Every prototype element enumerated: **221**. Eleven are excluded from the denominator with a
reason — **6 NOT-IN-PROTOTYPE** (the prototype does not implement them either) and **5
OUT-OF-SCOPE** (§4.12, which the spec says *"do not build"*). That leaves **210 measurable rows**,
of which **27 are DELIBERATE** — a divergence or omission with a recorded decision, which is not a
gap and must not be counted as one.

> # **125 of 183 prototype elements present (68%)**
>
> *(183 = 210 measurable rows minus the 27 with a recorded decision behind them.)*
>
> | | Count | Share of 183 |
> |---|---:|---:|
> | **BUILT** — exists with a `file:line` | **125** | **68.3%** |
> | **PARTIAL** — exists, states/mounts missing | **28** | 15.3% |
> | **ABSENT** — genuinely not there | **30** | 16.4% |
> | *present in some form (BUILT + PARTIAL)* | *153* | *83.6%* |
>
> Counted against all 210 measurable rows including the deliberate omissions:
> **125 BUILT (59.5%)**, **153 present (72.9%)**.
>
> Separately: **27 deliberately omitted or diverged** · **6 not built in the prototype either** ·
> **5 out of scope by the spec's own instruction**.

### 13b. Per section

| SPEC § | Surface | Rows | BUILT | PARTIAL | ABSENT | DELIB | BUILT % (of non-deliberate) |
|---|---|---:|---:|---:|---:|---:|---:|
| 4.1 | JD analysis — extraction | 32 | 13 | 3 | 8 | 8 | 54% |
| 4.2 | JD analysis — posting vs profile | 14 | 10 | 3 | 1 | 0 | 71% |
| 4.3 | ATS analysis modal | 13 | 6 | 2 | 3 | 2 | 55% |
| 4.4 | Asset steps — card + header | 33 | 24 | 7 | 0 | 2 | 77% |
| 4.5 | Field blocks | 42 | 32 | 1 | 2 | 7 | 91% |
| 4.6 | Keyword detail panel | 11 | 5 | 1 | 3 | 2 | 56% |
| 4.7 | Inline "Ask for a change" | 9 | 7 | 1 | 1 | 0 | 78% |
| 4.8 | QC & evidence step | 25 | 14 | 5 | 2 | 4 | 67% |
| 4.9 | Per-asset QC drawer | 14 | 12 | 1 | 0 | 1 | 92% |
| 4.10 | Review & send | 8 | 2 | 2 | 4 | 0 | **25%** |
| 4.11 | Assistant | 9 | 0 | 2 | 6 | 1 | **0%** |
| 4.12 | Prototype-only mode | *(5)* | — | — | — | — | out of scope |
| **Total** | | **210** | **125** | **28** | **30** | **27** | **68%** |

Coverage is **not** evenly distributed. The two steps the reader spends their time on — the asset
step (§4.4 + §4.5, 75 rows) and the QC surfaces (§4.8 + §4.9, 39 rows) — are at **74%** and
**74%**. The two ends of the journey are where the gaps are: the JD step's evidence expansion
(§4.1) and the send step (§4.10) plus the assistant (§4.11).

### 13c. THE DELTA — against `COMPONENT-INVENTORY.md`, 2026-08-24

This is the only prior measurement that is comparable: it measured **components and their states**
on the resume/asset step (§4.4 + §4.5), which is the same instrument this document uses. Its 21
sections were re-read against today's code and re-scored on **its own scale**:

| Inventory § | Component | 2026-08-24 | 2026-08-25 | Moved? |
|---|---|---|---|---|
| §1 | `Marked` — in-draft highlighting | ABSENT | **BUILT** | ▲ |
| §2 | `Rule` — measurement + target, state colour | PARTIAL | **BUILT** | ▲ |
| §3 | `KeyChip` | BLOCKED | **BUILT** | ▲ |
| §4 | `KeyDetail` | BLOCKED | **BUILT** | ▲ |
| §5 | `EchoTrail` — figures / Changes made | BUILT | BUILT | = |
| §6 | `EchoTrail` — phrases / Wording kept | PARTIAL | PARTIAL | = |
| §7 | `ReqChip` | BUILT | BUILT | = |
| §8 | `ReqLegend` | BUILT | BUILT | = |
| §9 | `AssetHeader` collapsed | BUILT | BUILT | = |
| §10 | `AssetHeader` four `Cell` stats | PARTIAL | PARTIAL | = |
| §11 | `AssetHeader` attention list | PARTIAL | **BUILT** (relocated to the field margin) | ▲ |
| §12 | "Already corrected in this asset" | BUILT | BUILT | = |
| §13 | `FieldList` | BUILT | BUILT | = |
| §14 | `PickList` | ABSENT | ABSENT | = |
| §15 | `AskBox` | BUILT | BUILT | = |
| §16 | `AssetBlock` card (`sameAsBefore`, focus ring) | PARTIAL | **BUILT** | ▲ |
| §17 | Margin provenance line + pass badge | BUILT | BUILT | = |
| §18 | "Claimed but not in the text" | BLOCKED | **BUILT** | ▲ |
| §19 | Hovered term's posting line | BUILT | BUILT | = |
| §20 | `AssetDocView` | BUILT | BUILT | = |
| §21 | `GateBadge` on the artifact card | PARTIAL | **BUILT** | ▲ |

| Measure | 2026-08-24 | 2026-08-25 | Delta |
|---|---:|---:|---:|
| **BUILT** | 10 / 21 (**47.6%**) | **18 / 21 (85.7%)** | **+8 components, +38.1 pts** |
| PARTIAL | 6 | 2 | −4 |
| ABSENT | 2 | 1 | −1 |
| BLOCKED (no data source) | 3 | **0** | **−3** |
| present in some form | 16 / 21 (76.2%) | **20 / 21 (95.2%)** | **+19.0 pts** |

**The inventory's own ranked build order, rows 1–8, is entirely closed.** Rows 1 (`Marked`),
2 (focus ring), 3 (per-field findings), 4 (state colour), 5 (`GateBadge` + `Approve` disabled),
6 (asset-level ask), 7 (`sameAsBefore`) and 8 ("Nothing to review on this asset.") all now cite a
`file:line`. Rows 9 (per-kind stats) and 12 (`PickList`) remain, and row 10 (the `rewording`
toggle) was **decided against** rather than left open. **All three BLOCKED rows are gone** — Row 11
Phase A/B shipped the keyword chips off `requirement.model_keyword`, not off the term library.

### 13d. The other prior instrument, and why it gives no delta

`UI-GAP-REGISTER.md`'s **165 panels / 19 controls** is a rendered-text diff. It cannot be compared
to either component count, and its own file says why (`:31-44`, `COMPONENT-INVENTORY.md:733`):
65% of its `resume` rows are SafetyIQ demo values, the components behind its "measurement strings"
are built, and one row measures a banned string that can never close. **Quoting a movement in that
number would be the error that file warns about in its own header.** It should be regenerated
(`node scripts/compare-ui.mjs --all …`) if the owner wants it current, but it will not answer this
question.

---

## 14. THE TEN HIGHEST-VALUE ABSENT ROWS

Ranked by what a user would notice first, not by effort.

| # | Row(s) | SPEC | What the user loses | Cheap or expensive |
|---:|---|---|---|---|
| 1 | **4.1-14 → 4.1-20** — the whole evidence expansion (status dot, `evidenced — show the line`, `no evidence found`, the excerpt, its source, its note, `Where it is used →`) | §4.1 | **Seven rows, one feature, and it is the spec's own acceptance criterion for the step** — *"every 'evidenced' claim can be expanded to a quote plus source"*. The JD step lists what the posting asks and never says whether the profile can answer it. R2 ("evidence or escalate") has no surface. | The data exists — `D:evidence-resolves-nothing` is **CLOSED** (`.claude/DEFERRED.md:131`), 1→6 evidenced on the live Trinnex posting. This is a renderer plus an endpoint field, not a new system. |
| 2 | **4.10-4, 4.10-6, 4.10-7** — the packet gate card, its per-item rows, and their `Open field →` | §4.10 | The last screen before sending says *"Approve all artifacts above"* and nothing about **what** is blocking or **where**. The reader is sent back to hunt. | **Cheapest high-value fix in the document.** `qcEntries`, `GateBadge`, `packetReadiness` and `onGoToField` are all already imported into `PacketBuilder.jsx` and in scope at `:719`. |
| 3 | **4.10-2** — the gate badge on the Review & send list | §4.10 | Five rows of `status` pills that say `review` with no word about whether the checks pass. Exactly the defect that was fixed on the artifact card (`PacketBuilder.jsx:184`) and not here. | One line. The component is imported at `:15`. |
| 4 | **4.8-10** — the `Needs a decision` list, on the page | §4.8 | SPEC §4.8 is explicit that the two lists are *"on the page, not behind a tab or a search"*. `Done for you` is (`QcRail.jsx:808`); its counterpart is not. The reader sees what was settled and must go tab-hunting for what was not. | Medium. `railAttention`/`attentionSplit` already produce the rows; the ordering rule (§5) is encoded. |
| 5 | **4.11-1 → 4.11-8** — the assistant panel | §4.11 | Six ABSENT rows. **But read §12 first** — this may be a deliberate architectural replacement, and if it is, it is not a gap at all. It is ranked here because it is unresolved, not because it is proven missing. | Large — or zero, depending on the owner's answer. |
| 6 | **4.2-1** — the four fit cards | §4.2 | The comparison's headline. A reader lands on the JD step and gets a table; the *"Responsibilities 4/4 · Must-have 5/7"* summary that makes the table scannable is not there. | Small. The grades are already per-row (`postingAnalysis.js:78-81`) and the summary line already counts strong/moderate/weak (`PostingAnalysis.jsx:171-179`). |
| 7 | **4.6-9, 4.6-10, 4.6-11** — the keyword panel's escape hatches (`Swap for another skill…`, `Drop it, leave the line open`, and the coverage-consequence phrasing) | §4.6 | SPEC §4.6 exists to answer *"what if I am not comfortable claiming this?"*. The panel now opens and explains; it offers no way out. | Medium — needs the profile's skill bank reachable from this surface. The **phrasing** pattern already exists at `AssetBlocks.jsx:495-502`. |
| 8 | **4.3-9, 4.3-10, 4.3-11** — the QC summary inside the ATS modal | §4.3 | The modal the header score opens is keywords-only. The prototype's version is the packet's at-a-glance verdict. Lower rank because the QC step and the artifact-card badges now carry all of it — this is duplication the app deliberately avoids (D4). | Low value, possibly correct as-is. |
| 9 | **4.5-40** — `{{merge field}}` placeholders shown inline in static blocks | §4.5 | SPEC §4.5 asks for them explicitly *"so the user can see where merged text lands"*. Without them a template block is an opaque paragraph and the reader cannot tell what will be substituted. | Small, and it is a real comprehension gain on the letterhead / compact-resume blocks. |
| 10 | **4.5-12** — `PickList` (`type: 'select'` fields) | §4.5 | Portfolio only, so **no resume impact** — which is why it is last despite being a whole missing body shape. `shapeOf()` (`assetBlocks.js:144-151`) has no `select` branch. | Needs per-item candidacy on the insertions payload (which items were available, chosen, blocked), not just the joined text. |

**Just outside the ten**, and both one-liners: **4.1-3** (`See where each one is answered →`, the
JD step's only route into QC) and **4.8-21** (`Ask why` on a swap row).

---

## 15. WHAT THIS METHOD PROVES, AND WHAT IT DOES NOT

**Reading code proves a component exists in source. It does not prove it renders correctly at
runtime, with this owner's real data, in a browser.** Every BUILT verdict above should be read as
*"the component and its states are in the source at this line"* — never as *"a user sees this
today"*.

Three specific limits, and what would close each:

**1. A component can be built and never reach the screen for want of data.** Half of §4.5's newest
rows depend on payload shape: the keyword chips need `requirement.model_keyword` to be non-empty on
this opportunity, the findings list needs `findingsByField(result)` to return rows, the state
colour needs `fieldSev` to be non-null. A field with no findings correctly renders *no colour* —
which is indistinguishable, by reading, from a colour that never renders. **These need
`npm run test:margin` (`app/test/browser/run-field-margin.mjs`) — it renders the field margin for
real.**

Rows that need it: **4.5-6** (state colour), **4.5-13/-14/-15** (highlighting and the linked
hover), **4.5-23** and **4.4-28** (the findings list), **4.5-28/-31/-36** (chips, detail panel,
`not in this text`), **4.4-32** (`Nothing to review on this asset.`), **4.5-18** (the `identical`
original state).

**2. A component can be built and never reach the screen for want of a mount.** This document read
every relevant import list, which catches the `CorrectionRow` class of error — but a mount inside a
branch that never evaluates true would still read as BUILT. **These need `ui-verify.yml` against
the live app**, which executes the bundle and asserts on rendered text.

Rows that need it: **4.4-3** (`GateBadge` on the card), **4.4-11** (`Approve` disabled — needs a
packet whose gate actually fails), **4.4-13** (asset-level `List Tweaks`), **4.5-41** (the focus
ring, which needs a deep link followed end to end), **4.3-13** (the modal dismissing before it
navigates — pure runtime ordering, unprovable by reading), **4.7-7** (the in-place confirmation).

Suggested invocation, following `CLAUDE.md`'s pattern:
```
workflow_id="ui-verify.yml", ref="main",
inputs={ "route": "#/packet/<id>", "owner": "von.ellis@enterpriseds.io",
         "expect": "Corrected for you;Open on this field;proposed" }
```

**3. Three classification judgements a reader should check rather than trust:**

- **§4.11 (the assistant)** — the biggest single call in this document. Six ABSENT rows rest on
  reading `AssetBlocks.jsx:495`, a *code comment*, as an architectural decision. `accuracy-log.md`'s
  first entry is exactly the failure of doing that. **It is not recorded in `.claude/actions.md`.**
  If the owner confirms the replacement, the headline moves from 68% to roughly **72%**.
- **"Relocated, not absent"** was used for six rows (4.4-28, 4.4-30, 4.5-23, 4.6-8, 4.9-12,
  4.3-11). Each cites a real mount elsewhere, and in four cases a recorded reason. A reader who
  thinks a relocation is a gap should re-score those six; the headline would fall by about three
  points.
- **`DELIBERATE` rests on a citation in every one of its 27 rows** — `PULL-CANDIDATES.md` PC-1
  through PC-5, `.claude/DEFERRED.md:191` (`D:term-library-off-by-owner-decision`),
  `.claude/actions.md:2947-2951`, `assetGate.js:78-87`, and `COMPONENT-INVENTORY.md:249-255`. Of
  those, **17 of the 27 trace to the single term-library decision.** If the owner turns the library
  on, 17 rows re-enter the denominator as work, and the headline falls to about **63%** until they
  are built.

**What was NOT done:** no workflow was dispatched, no live UI was verified, nothing was run. This
is a source measurement, honestly labelled as one.

---

*Measurement only. Nothing under `app/src` or `api/src` was read-modified; this file is the only
one written. No fix was applied to anything reported here.*
